# UX4AI

**Interactive demo site (static)** — интерактивный веб-сайт с 3D-сценой, HUD-панелями и туром.

## 📖 О проекте

UX4AI — это интерактивный веб-сайт, демонстрирующий методологию UX для AI-продуктов через:

- **🎮 3D-сцена** (`calmScene-complete`) — интерактивная визуализация узлов знаний в трёхмерном пространстве
- **📱 HUD (Head-Up Display)** — панели с контентом, открывающиеся при клике на узлы 3D-сцены
- **🎯 Тур** — пошаговый walkthrough для новых пользователей
- **🎨 Темы** — переключение между Dark и Light темами с синхронизацией UI и 3D-сцены

**Технологии:**
- Three.js для 3D-рендеринга
- Модульная архитектура ES6
- Централизованная система тем (Dark/Light)
- Service Worker для кэширования

---

## 📚 Документация

**⚠️ ВАЖНО: Актуальная документация находится только в указанных ниже файлах. Остальные README-* файлы перенесены в `docs/legacy/`.**

### Основные документы

- **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** — целевая архитектура проекта, подсистемы, инварианты
- **[docs/PROJECT_MAP.md](docs/PROJECT_MAP.md)** — карта проекта, структура директорий, описание файлов
- **[THEME-SYSTEM.md](THEME-SYSTEM.md)** — архитектура системы тем
- **[docs/COLOR_MAP.md](docs/COLOR_MAP.md)** — документация всех цветовых токенов (единственный источник документации по цветам)
- **[THEME-CUSTOMIZATION-GUIDE.md](THEME-CUSTOMIZATION-GUIDE.md)** — руководство по настройке тем

### Краткие обзоры

- **[QUICK-START.md](QUICK-START.md)** — быстрый старт для разработчиков

### Legacy документация

Старые README-* файлы и отчёты перенесены в [`docs/legacy/`](docs/legacy/) для справки.

---

## 🚀 Быстрый старт

### Локальная разработка

**✅ Гибкость: сервер можно запускать из любой директории**

Приложение автоматически определяет базовый путь через `window.__UX4AI_BASE_PATH__`:
- Если `location.pathname` содержит `/UX4AI/` → базовый путь = `'/UX4AI'`
- Иначе → базовый путь = `''` (корень)

**Варианты запуска:**

**1. Из папки UX4AI (рекомендуется):**
```bash
cd UX4AI
npx serve -l 5173 .
# Откройте http://localhost:5173/
```

**2. Из родительского каталога:**
```bash
cd ..
npx serve -l 5173 .
# Откройте http://localhost:5173/UX4AI/
```

Все пути (модули, прелоадер, статические ресурсы) вычисляются автоматически.

### Деплой на GitHub Pages

См. [`DEPLOY.md`](DEPLOY.md) для деталей. TL;DR:

```bash
git init && git add . && git commit -m "init"
git branch -M main
git remote add origin https://github.com/<your-username>/ux4ai.git
git push -u origin main
```

Затем включите Pages: Settings → Pages → Source: `main` / `(root)`.

---

## 🎨 Работа с цветами

**Правило изменений:**
> Любые новые цвета → сначала в [`docs/COLOR_MAP.md`](docs/COLOR_MAP.md), затем в `core/theme-colors.js`

**Запрещено:**
- ❌ Добавлять новые hex/rgb/hsl в UI/3D без привязки к токену
- ❌ Создавать новые документы про цвета

**Разрешено:**
- ✅ Использовать прямые цвета только в `core/theme-colors.js`
- ✅ Использовать токены везде (`getThemeColors()`, `getSceneColors()`, CSS переменные)

Подробнее см. [`docs/COLOR_MAP.md`](docs/COLOR_MAP.md) (раздел "Правило изменений").

---

## 📁 Структура проекта

### Основные директории

- **`core/`** — Ядро 3D-движка и базовые системы
  - `engine.js` — главный движок Three.js
  - `hud-manager.js` — менеджер HUD-панелей
  - `tour.js` — модуль тура (Фаза 3)
  - `theme-colors.js` — централизованная система цветов
  - Другие модули: `navigation.js`, `picking.js`, `labels.js`, `sections.js`, `state.js`

- **`ui/`** — UI компоненты и контроллеры
  - `theme-controller.js` — центральный контроллер тем
  - `themeSwitcher.js` — UI-обертка для переключения тем

- **`scenes/`** — 3D-сцены (плагины)
  - `calmScene-complete.js` — единственная активная 3D-сцена

- **`themes/`** — Конфигурации тем и сцен
  - `calm.js` — конфигурация сцены "Calm"
  - `index.js` — реестр сцен (содержит только `calm`)
  - `ui-theme-config.js` — конфигурации UI-тем (Dark/Light)

- **`data/`** — Данные контента
  - `nodes-data-complete.js` — данные узлов 3D-сцены

- **`styles/`** — CSS стили и дизайн-токены
  - `tokens.css` — дизайн-токены Synaptik Design System
  - `main.css` — основные стили
  - `variables.css` — legacy alias-слой (для обратной совместимости)

- **`docs/`** — Документация
  - `ARCHITECTURE.md` — целевая архитектура проекта
  - `PROJECT_MAP.md` — карта проекта, структура директорий
  - `COLOR_MAP.md` — документация всех цветовых токенов
  - `legacy/` — архив устаревших документов

- **`archive/`** — Архив неиспользуемых файлов
  - Неиспользуемые медиа-файлы, старые модули

### Подробная документация

- **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** — полная архитектура проекта, подсистемы, инварианты
- **[docs/PROJECT_MAP.md](docs/PROJECT_MAP.md)** — детальная карта проекта, описание всех файлов
- **[THEME-SYSTEM.md](THEME-SYSTEM.md)** — архитектура системы тем
- **[docs/COLOR_MAP.md](docs/COLOR_MAP.md)** — документация всех цветовых токенов

---

## 📖 Для разработчиков

- **Добавить новый цвет:** [`docs/COLOR_MAP.md`](docs/COLOR_MAP.md) → раздел "Руководство для разработчиков"
- **Изменить архитектуру:** [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) → раздел "Добавление новых компонентов"
- **Настроить тему:** [`THEME-CUSTOMIZATION-GUIDE.md`](THEME-CUSTOMIZATION-GUIDE.md)
