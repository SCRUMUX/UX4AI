# Карта цветов проекта UX4AI

**Версия:** Фаза 5 — Единственный документационный источник  
**Дата создания:** 2024
**Последнее обновление:** Фаза 5 — Закрытие двойной правды

---

## ⚠️ ВАЖНО: Единственный источник документации по цветам

**Этот документ (`docs/COLOR_MAP.md`) — единственный источник документации по цветам проекта.**

**Источники истины (runtime):**
- **UI цвета:** `styles/tokens.css` — CSS-переменные для всех UI-компонентов (маппятся на токены из `core/theme-colors.js`)
- **3D-сцена:** `core/theme-colors.js` — JavaScript-модуль с цветами для Three.js сцены
- **Конфигурация тем:** `themes/ui-theme-config.js` — конфигурации Dark/Light тем (CSS-классы, meta-теги)
- **Применение тем:** `ui/theme-controller.js` — единая точка применения тем

**Правило изменений:**
> **Любые новые цвета → сначала сюда (`COLOR_MAP.md`), затем в `core/theme-colors.js`**

**Запрещено:**
- ❌ Добавлять новые hex/rgb/hsl в UI/3D без привязки к токену
- ❌ Создавать новые документы про цвета
- ❌ Дублировать информацию о цветах в других файлах

---

## Введение

Этот документ описывает **нормализованную модель тем** для проекта UX4AI. Цель — превратить COLOR_MAP.md + theme-colors.js в настоящую дизайн-систему тем, а не набор случайных цветов.

**Структура модели:**
1. **Базовая палитра** — нейтральные цвета, акценты, статусы
2. **Семантические роли интерфейса** — фон, панели, текст, интерактив, состояния
3. **3D-пакет** — фон сцены, сетка/орбиты, базовая сфера, динамические эффекты

---

## 1. Базовая палитра

### 1.1 Нейтральные цвета

**Назначение:** Базовые цвета для фонов, поверхностей и текста. Не зависят от акцентов.

#### Dark Theme (Тёмная тема)

| Токен | Значение | Роль | Статический/Динамический |
|-------|----------|------|--------------------------|
| `--Color/Dark/Background/Primary` | `#0D1117` | Основной фон страницы | ✅ Статический |
| `--Color/Dark/Background/Secondary` | `#161B22` | Вторичный фон | ✅ Статический |
| `--Color/Dark/Background/Tertiary` | `#0F1419` | Третичный фон | ✅ Статический |
| `--Color/Light/Text/Primary` | `#F0F6FC` | Основной текст (светлый на тёмном) | ✅ Статический |
| `--Color/Light/Text/Secondary` | `#D4E4F7` | Вторичный текст | ✅ Статический |
| `--Color/Light/Text/Tertiary` | `#A5B4C6` | Третичный текст | ✅ Статический |
| `--Color/Light/Text/Muted` | `#8B98A5` | Приглушённый текст | ✅ Статический |
| `--Color/Dark/Border/Default` | `#30363D` | Основная граница | ✅ Статический |
| `--Color/Dark/Border/Subtle` | `rgba(48, 54, 61, 0.5)` | Приглушённая граница | ✅ Статический |

#### Light Theme (Светлая тема)

| Токен | Значение | Роль | Статический/Динамический |
|-------|----------|------|--------------------------|
| `--Color/Light/Background/Base` | `#FAFBFC` | Основной фон страницы | ✅ Статический |
| `--Color/Light/Background/Muted` | `#F6F8FA` | Приглушённый фон | ✅ Статический |
| `--Color/Light/Text/Base` | `#1F2328` | Основной текст (тёмный на светлом) | ✅ Статический |
| `--Color/Light/Text/Muted` | `#656D76` | Приглушённый текст | ✅ Статический |
| `--Color/Light/Border/Base` | `#D8DEE4` | Основная граница | ✅ Статический |

### 1.2 Акцентные цвета

**Назначение:** Брендовые цвета для интерактивных элементов, ссылок, выделений.

| Токен | Dark Theme | Light Theme | Роль | Статический/Динамический |
|-------|------------|-------------|------|--------------------------|
| `--Color/Accent/Primary` | `#58A6FF` | `#58A6FF` | Основной акцент | ✅ Статический |
| `--Color/Accent/Hover` | `#4493E6` | `#4493E6` | Акцент при наведении | ✅ Статический |
| `--Color/Accent/Active` | `#3582D4` | `#3582D4` | Акцент при нажатии | ✅ Статический |

**Примечание:** Акцентные цвета одинаковы для обеих тем, обеспечивая консистентность бренда.

### 1.3 Статусные цвета

**Назначение:** Цвета для состояний элементов (success, warning, error, info).

**Статус:** ⚠️ **Требуется добавление** — в текущей версии статусные цвета не определены отдельно, используются акценты.

**Рекомендация:** Добавить токены для статусов:
- `--Color/Status/Success`
- `--Color/Status/Warning`
- `--Color/Status/Error`
- `--Color/Status/Info`

---

## 2. Семантические роли интерфейса

### 2.1 Фон страницы

**Назначение:** Основной фон приложения и 3D-сцены.

| Токен | Dark Theme | Light Theme | Где используется | Статический/Динамический |
|-------|------------|-------------|------------------|--------------------------|
| `--Color/Dark/Background/Primary` | `#0D1117` | — | Фон `<body>`, фон 3D-сцены | ✅ Статический |
| `--Color/Light/Background/Base` | — | `#FAFBFC` | Фон `<body>`, фон 3D-сцены | ✅ Статический |

### 2.2 Поверхности (Панели, карточки, оверлеи)

**Назначение:** Поверхности для контента, панелей, модальных окон.

#### Dark Theme

| Токен | Значение | Роль | Где используется | Статический/Динамический |
|-------|----------|------|------------------|--------------------------|
| `--Color/Dark/Surface/Base` | `rgba(22, 27, 34, 0.92)` | Базовая поверхность | Панели с прозрачностью | ✅ Статический |
| `--Color/Dark/Surface/Elevated` | `rgba(22, 27, 34, 0.65)` | Приподнятая поверхность | HUD-панели, модальные окна | ✅ Статический |
| `--Color/Dark/Surface/Ghost` | `rgba(22, 27, 34, 0.18)` | Полупрозрачная поверхность | Хедер, фоновые элементы | ✅ Статический |
| `--Color/Dark/Surface/Overlay` | `rgba(7, 10, 15, 0.65)` | Оверлей | Фон тура, HUD-бэкдроп | ✅ Статический |
| `--Color/Dark/Surface/Panel` | `rgba(22, 27, 34, 0.40)` | Панель (десктоп) | HUD-панели, карточки тура | ✅ Статический |
| `--Color/Dark/Surface/PanelMobile` | `rgba(22, 27, 34, 0.55)` | Панель (мобильные) | HUD-панели на мобильных | ✅ Статический |

#### Light Theme

| Токен | Значение | Роль | Где используется | Статический/Динамический |
|-------|----------|------|------------------|--------------------------|
| `--Color/Light/Surface/1` | `#FFFFFF` | Поверхность 1 | Базовые панели | ✅ Статический |
| `--Color/Light/Surface/2` | `#F6F8FA` | Поверхность 2 | Приподнятые панели | ✅ Статический |
| `--Color/Light/Surface/3` | `#F0F3F6` | Поверхность 3 | Фоновые элементы | ✅ Статический |
| `--Color/Light/Surface/Panel` | `rgba(255, 255, 255, 0.95)` | Панель (десктоп) | HUD-панели, карточки тура | ✅ Статический |
| `--Color/Light/Surface/PanelMobile` | `rgba(247, 248, 250, 0.95)` | Панель (мобильные) | HUD-панели на мобильных | ✅ Статический |

### 2.3 Текст (Основной, вторичный, слабый, ссылки)

**Назначение:** Цвета текста для различных уровней иерархии.

#### Dark Theme (светлый текст на тёмном фоне)

| Токен | Значение | Роль | Где используется | Статический/Динамический |
|-------|----------|------|------------------|--------------------------|
| `--Color/Light/Text/Primary` | `#F0F6FC` | Основной текст | Заголовки, основной контент | ✅ Статический |
| `--Color/Light/Text/Secondary` | `#D4E4F7` | Вторичный текст | Подзаголовки, мета-информация | ✅ Статический |
| `--Color/Light/Text/Tertiary` | `#A5B4C6` | Третичный текст | Вспомогательный текст | ✅ Статический |
| `--Color/Light/Text/Muted` | `#8B98A5` | Приглушённый текст | Неактивный текст, подсказки | ✅ Статический |
| `--Color/Light/Text/OnBrand` | `#FFFFFF` | Текст на акцентном фоне | Кнопки, бейджи | ✅ Статический |

#### Light Theme (тёмный текст на светлом фоне)

| Токен | Значение | Роль | Где используется | Статический/Динамический |
|-------|----------|------|------------------|--------------------------|
| `--Color/Light/Text/Base` | `#1F2328` | Основной текст | Заголовки, основной контент | ✅ Статический |
| `--Color/Light/Text/Muted` | `#656D76` | Приглушённый текст | Вспомогательный текст | ✅ Статический |
| `--Color/Light/Text/OnBrand` | `#FFFFFF` | Текст на акцентном фоне | Кнопки, бейджи | ✅ Статический |

**Ссылки:** Используют `--Color/Accent/Primary` для обычного состояния и `--Color/Accent/Hover` для hover.

### 2.4 Интерактив (Кнопки, hover, focus, disabled)

**Назначение:** Цвета для интерактивных элементов и их состояний.

| Токен | Значение | Роль | Где используется | Статический/Динамический |
|-------|----------|------|------------------|--------------------------|
| `--Color/State/Hover/Background` | `rgba(88, 166, 255, 0.10)` | Фон при наведении | Hover-состояния кнопок, ссылок | ✅ Статический |
| `--Color/State/Active/Background` | `rgba(88, 166, 255, 0.18)` | Фон при нажатии | Active-состояния кнопок | ✅ Статический |
| `--Color/State/Selected/Background` | `rgba(88, 166, 255, 0.14)` | Фон выбранного элемента | Выбранные вкладки, пункты меню | ✅ Статический |
| `--Color/State/Hover/Surface` | `rgba(48, 54, 61, 0.95)` (Dark) / `rgba(247, 248, 250, 0.95)` (Light) | Поверхность при наведении | Hover на панелях | ✅ Статический |

**Focus:** Использует `--Color/Accent/Primary` для outline/border.

**Disabled:** Использует `--Color/Light/Text/Muted` (Dark) или `--Color/Light/Text/Muted` (Light) с пониженной непрозрачностью.

### 2.5 Состояния (Success, Warning, Error, Info)

**Назначение:** Цвета для статусных сообщений и индикаторов.

**Статус:** ⚠️ **Требуется добавление** — в текущей версии используются акценты.

**Рекомендация:** Добавить токены для состояний (см. раздел 1.3).

---

## 3. 3D-пакет

### 3.1 Фон сцены

**Назначение:** Основной фон 3D-сцены в Three.js.

| Токен (JS) | CSS-переменная | Dark Theme | Light Theme | Где используется | Статический/Динамический |
|------------|----------------|------------|-------------|-----------------|--------------------------|
| `sceneBg` | `--scene-bg` | `#0D1117` | `#FAFBFC` | `scene.background` в Three.js | ✅ Статический |

**Примечание:** Фон сцены совпадает с основным фоном страницы для визуальной целостности.

### 3.2 Сетка/Орбиты

**Назначение:** Цвета для сетки координат и орбитальных линий.

| Токен (JS) | Dark Theme | Light Theme | Где используется | Статический/Динамический |
|------------|------------|-------------|-----------------|--------------------------|
| `sceneGrid` | `#8B98A5` | `#656D76` | Сетка координат, орбитальные линии | ✅ Статический |
| `sceneSpace` | `#1A2332` | `#E8EDF2` | Фоновое пространство сцены | ✅ Статический |

### 3.3 Базовая сфера

**Назначение:** Цвет центральной сферы в сцене.

| Токен (JS) | Dark Theme | Light Theme | Где используется | Статический/Динамический |
|------------|------------|-------------|-----------------|--------------------------|
| `sceneCore` | `#4A9EFF` | `#4493E6` | Центральная сфера | ✅ Статический |

**Примечание:** Цвет сферы близок к акцентному, но немного отличается для визуального выделения.

### 3.4 Диапазон для динамических эффектов

**Назначение:** Цвета для импульсов, линий, вспышек и других динамических эффектов. Эти цвета могут варьироваться в зависимости от акцентного цвета.

#### Базовые цвета эффектов

| Токен (JS) | Dark Theme | Light Theme | Роль | Статический/Динамический |
|------------|------------|-------------|------|--------------------------|
| `sceneAccent` | `#58A6FF` | `#58A6FF` | Базовый акцентный цвет сцены | ✅ Статический |
| `impulseColor` | `#58A6FF` | `#58A6FF` | Цвет импульсов при взаимодействии | ✅ Статический (база для динамики) |

#### Лучи и связи

| Токен (JS) | Dark Theme | Light Theme | Роль | Статический/Динамический |
|------------|------------|-------------|------|--------------------------|
| `beamBase` | `#6BB0FF` | `#58A6FF` | Базовый цвет лучей | ✅ Статический |
| `beamLine` | `#B8D9FF` | `#B8D9FF` | Цвет линий лучей | ✅ Статический |
| `beamInner` | `#9FC5F0` | `#A8D0FF` | Внутренний цвет лучей | ✅ Статический |
| `beamOuter` | `#5BA3FF` | `#4493E6` | Внешний цвет лучей | ✅ Статический |
| `beamA` | `#7BC8FF` | `#6BB0FF` | Цвет луча A | ✅ Статический |
| `beamB` | `#9BC5FF` | `#8BC5FF` | Цвет луча B | ✅ Статический |

**Примечание:** Цвета лучей варьируются от базового акцента для создания визуального разнообразия, но остаются статическими.

#### Пульсации и вспышки

| Токен (JS) | Dark Theme | Light Theme | Роль | Статический/Динамический |
|------------|------------|-------------|------|--------------------------|
| `pulse` | `#2E5F8A` | `#3582D4` | Цвет пульсации (базовый) | ✅ Статический (база для динамики) |
| `pulseB` | `#3E7AB5` | `#4493E6` | Цвет пульсации B | ✅ Статический (база для динамики) |

**Примечание:** Пульсации используют статические базовые цвета, но интенсивность и прозрачность могут изменяться динамически во время анимации.

#### Звёзды и туман

| Токен (JS) | Dark Theme | Light Theme | Роль | Статический/Динамический |
|------------|------------|-------------|------|--------------------------|
| `starNear` | `#58A6FF` | `#58A6FF` | Цвет ближних звёзд | ✅ Статический |
| `starFar` | `#6B7A8A` | `#8B98A5` | Цвет дальних звёзд | ✅ Статический |
| `mist` | `#9FC5F0` | `#A8D0FF` | Цвет тумана | ✅ Статический |

#### Освещение

| Токен (JS) | Dark Theme | Light Theme | Роль | Где используется | Статический/Динамический |
|------------|------------|-------------|------|------------------|--------------------------|
| `lightHemi` | `#B8D4FF` | `#E6F2FF` | Цвет полусферического света | `HemisphereLight.color` | ✅ Статический |
| `lightHemiGround` | `#0F1419` | `#F6F8FA` | Цвет земли полусферического света | `HemisphereLight.groundColor` | ✅ Статический |
| `lightDir` | `#F0F6FC` | `#1F2328` | Цвет направленного света | `DirectionalLight.color` | ✅ Статический |

#### Палитра узлов

| Токен (JS) | Значение | Роль | Статический/Динамический |
|------------|----------|------|--------------------------|
| `nodePalette` | Массив из 8 цветов | Палитра цветов узлов | ✅ Статический |

**Цвета палитры:**
- `#58A6FF` (blue) — совпадает с акцентом
- `#2DD4BF` (green)
- `#F59E0B` (orange)
- `#F87171` (red)
- `#A78BFA` (purple)
- `#14B8A6` (teal)
- `#F472B6` (pink)
- `#84CC16` (lime)

**Примечание:** Палитра узлов статическая, но узлы выбирают цвета из палитры динамически для разнообразия.

---

## Классификация токенов по типу использования

### ✅ Статические токены

**Определение:** Токены, значения которых строго заданы темой и не изменяются во время выполнения.

**Примеры:**
- Все цвета фонов (`bgBase`, `bgSecondary`, `bgTertiary`)
- Все цвета текста (`textPrimary`, `textSecondary`, `textMuted`)
- Все цвета акцентов (`accentPrimary`, `accentHover`, `accentActive`)
- Все цвета поверхностей (`surface1`, `surface2`, `surfacePanel`)
- Все цвета границ (`borderBase`, `borderSubtle`)
- Все цвета 3D-сцены (фон, сетка, базовая сфера, базовые цвета лучей)

**Использование:** Эти токены используются напрямую без модификаций.

### 🔄 Динамические токены (база для динамики)

**Определение:** Токены, которые используются как база для динамических эффектов, но сами остаются статическими.

**Примеры:**
- `impulseColor` — используется как база для импульсов, но интенсивность и прозрачность изменяются динамически
- `pulse`, `pulseB` — используются как база для пульсаций, но интенсивность изменяется во время анимации
- `beamBase`, `beamInner`, `beamOuter` — используются как база для лучей, но прозрачность и интенсивность могут изменяться

**Использование:** Эти токены используются как отправная точка для вычисления динамических значений (например, `rgba(impulseColor, dynamicAlpha)`).

### 📊 Динамический выбор из статической палитры

**Определение:** Токены, которые представляют палитру статических цветов, из которых выбирается цвет динамически.

**Примеры:**
- `nodePalette` — массив из 8 статических цветов, из которых узлы выбирают цвет динамически

**Использование:** `nodePalette[colorIndex]` — выбор цвета из палитры на основе индекса узла.

---

---

## Соответствие с `styles/tokens.css` и `core/theme-colors.js`

### Структура соответствия

**CSS-переменные (`styles/tokens.css`):**
- Определяют значения для UI-компонентов
- Используются в CSS через `var(--token-name)`
- Маппятся на значения из `core/theme-colors.js`

**JavaScript-токены (`core/theme-colors.js`):**
- Определяют значения для JavaScript-кода и 3D-сцены
- Используются через `getThemeColors()` и `getSceneColors()`
- Структурированы так же, как в COLOR_MAP.md

### Компонентные токены (Component-Specific)

Эти токены являются композиционными и маппятся на базовые токены:

| Компонентный токен | Маппится на | Где используется |
|-------------------|------------|------------------|
| `--tour-backdrop` | `--Color/Dark/Surface/Overlay` | Бэкдроп тура (overlay) |
| `--tour-card-bg` | `--Color/Dark/Surface/Panel` | Карточка тура (десктоп) |
| `--tour-card-bg-mobile` | `--Color/Dark/Surface/PanelMobile` | Карточка тура (мобильные) |
| `--tour-text` | `--Color/Light/Text/Primary` | Основной текст тура |
| `--tour-text-secondary` | `--Color/Light/Text/Secondary` | Вторичный текст тура |
| `--tour-text-muted` | `--Color/Light/Text/Muted` | Приглушённый текст тура |
| `--hud-backdrop` | `--Color/Dark/Surface/Overlay` | Бэкдроп HUD (overlay) |
| `--hud-panel-bg` | `--Color/Dark/Surface/Panel` | Панели HUD (десктоп) |
| `--hud-panel-bg-mobile` | `--Color/Dark/Surface/PanelMobile` | Панели HUD (мобильные) |
| `--hud-text` | `--Color/Light/Text/Primary` | Основной текст HUD |
| `--hud-text-secondary` | `--Color/Light/Text/Secondary` | Вторичный текст HUD |
| `--hud-text-muted` | `--Color/Light/Text/Muted` | Приглушённый текст HUD |
| `--scene-bg` | `--Color/Dark/Background/Primary` | Фон 3D-сцены |
| `--header-bg` | `--Color/Dark/Surface/Ghost` | Хедер сайта |
| `--header-bg-hud` | `--Color/Dark/Surface/Elevated` | Хедер при открытом HUD |
| `--header-border` | `--Color/Dark/Border/Default` | Граница хедера |

### Legacy Compatibility Tokens (Обратная совместимость)

Эти токены используются для обратной совместимости со старым кодом:

| Токен | Маппится на | Где используется |
|-------|------------|------------------|
| `--bg` | `--Color/Dark/Background/Primary` | Старый код, fallback |
| `--text` | `--Color/Light/Text/Primary` | Старый код, fallback |
| `--panel` | `--Color/Dark/Background/Tertiary` | Старый код, fallback |
| `--accent` | `--Color/Accent/Primary` | Старый код, fallback |
| `--border` | `--Color/Dark/Border/Default` | Старый код, fallback |
| `--muted` | `--Color/Light/Text/Muted` | Старый код, fallback |
| `--glow` | `--Color/Effect/Glow` | Старый код, fallback |
| `--panel-solid` | `--Color/Dark/Background/Secondary` | Старый код, fallback |
| `--panel-ghost` | `--Color/Dark/Surface/Ghost` | Старый код, fallback |
| `--shadow-soft` | `0 2px 10px var(--Color/Effect/Shadow/Soft)` | Старый код, fallback |

---

## Критические цвета страницы

### Meta Theme Color

**Источник:** `<meta name="theme-color">` в `index.html`

| Значение | Роль | Где используется |
|----------|------|------------------|
| `#0B0F14` (тёмная тема) | Цвет темы браузера | Android Chrome, Safari (цвет адресной строки) |
| Обновляется динамически | При смене темы через JavaScript | `meta-theme-color` элемент |

**Примечание:** Значение обновляется динамически при смене темы через `ui/theme-controller.js`.

### Preloader Colors (Прелоадер)

**Источник:** Inline стили в `index.html` (блок `#preloader-overlay`)

| Элемент | Цвет | Роль |
|---------|------|------|
| `#preloader-overlay` background | `#0B0F14` | Фон прелоадера (совпадает с фоном страницы) |

**Примечание:** Цвет жёстко задан в inline-стилях для предотвращения FOUC (Flash of Unstyled Content).

### Critical Colors (Критические цвета)

**Источник:** CSS-переменные в `<style>` блоке `index.html` (в `<head>`)

Эти переменные используются для мгновенного применения стилей при загрузке страницы (предотвращение FOUC):

| Переменная | Тёмная тема | Светлая тема | Роль |
|------------|-------------|--------------|------|
| `--critical-bg` | `#0B0F14` | `#FFFFFF` | Фон страницы (применяется к `<html>`) |
| `--critical-text` | `#E6EEF8` | `#0B0F14` | Цвет текста (применяется к `<html>`, `<body>`) |
| `--critical-header-bg` | `rgba(18, 23, 34, 0.15)` | `rgba(247, 248, 250, 0.95)` | Фон хедера |
| `--critical-logo-name` | `#CFE8FF` | `#0B0F14` | Цвет названия логотипа |
| `--critical-logo-tagline` | `#9AA6B2` | `#6B7280` | Цвет слогана логотипа |
| `--critical-btn` | `#9AA6B2` | `#6B7280` | Цвет кнопок в хедере |

**Примечание:** Эти переменные применяются с `!important` для гарантированного отображения до загрузки основных стилей.

---

## Legacy-носители цветов

Следующие файлы считаются **legacy** (кандидаты на удаление или миграцию):

### `themes/theme-variables.css`

**Статус:** Legacy — **НЕ ИСПОЛЬЗУЕТСЯ**  
**Описание:** Старые CSS-переменные для тёмной и светлой тем. Дублирует функциональность `styles/tokens.css`, но с другими именами переменных.

**Использование:** Файл не подключен в `index.html` и не используется в коде. **Готов к удалению.**

**PHASE C4:** Файл можно безопасно удалить, так как все переменные из него уже определены в `styles/tokens.css`.

### `themes/simple-themes.css`

**Статус:** Legacy — **НЕ ИСПОЛЬЗУЕТСЯ**  
**Описание:** Простые темы (светлая, неоновая). Содержит дублирующие определения цветов.

**Использование:** Файл не подключен в `index.html` и не используется в коде. Неоновая тема не используется в текущей версии. **Готов к удалению.**

**PHASE C4:** Файл можно безопасно удалить, так как все необходимые темы определены в `styles/tokens.css`.

### `styles/variables.css`

**Статус:** Legacy — **ИСПОЛЬЗУЕТСЯ (тонкий alias-слой)**  
**Описание:** Минимальный набор переменных для обратной совместимости. Содержит только базовые токены (`--bg`, `--text`, `--panel`, и т.д.).

**Использование:** Используется в `index.html` для обратной совместимости. **PHASE C4:** Преобразован в тонкий alias-слой — все переменные теперь напрямую маппятся на токены из `styles/tokens.css` без fallback-значений.

**Миграция:** Постепенно заменять использование `--bg`, `--text`, `--panel`, `--accent`, `--border`, `--muted` на прямые токены из `styles/tokens.css`. После полной миграции файл можно удалить.

**Важно:** Новые компоненты/экраны (HUD/scene/tour) **НЕ ДОЛЖНЫ** использовать эти legacy-переменные, только токены напрямую.

### Inline-стили в `index.html`

**Статус:** Частично legacy, частично необходимые  
**Описание:** Inline-стили для критических цветов (прелоадер, critical-* переменные).

**Использование:**
- **Критические цвета** (в `<head>`) — **необходимы** для предотвращения FOUC
- **Прелоадер** — можно мигрировать в CSS-файл, но inline допустим для критических стилей
- **Другие inline-стили** — кандидаты на миграцию в CSS-файлы

---

## Резюме

### Источники истины

1. **UI цвета:** `styles/tokens.css` — единственный источник CSS-переменных для UI
2. **3D-сцена:** `core/theme-colors.js` — единственный источник цветов для Three.js сцены
3. **Критические цвета:** Inline-стили в `index.html` (необходимы для предотвращения FOUC)

### Legacy файлы (PHASE C4 статус)

- `themes/theme-variables.css` — **НЕ ИСПОЛЬЗУЕТСЯ**, готов к удалению
- `themes/simple-themes.css` — **НЕ ИСПОЛЬЗУЕТСЯ**, готов к удалению
- `styles/variables.css` — **ИСПОЛЬЗУЕТСЯ** как тонкий alias-слой (все переменные маппятся на токены), можно удалить после полной миграции в `index.html`

### Рекомендации

1. **Новые цвета:** Добавлять только в `styles/tokens.css` (UI) или `core/theme-colors.js` (3D-сцена)
2. **Миграция:** Постепенно мигрировать использование legacy-файлов на `styles/tokens.css`
3. **Жёстко заданные цвета:** Избегать прямых hex/rgb значений в коде, использовать токены

---

## Текущий статус использования цветов

**Дата аудита:** Фаза 1 — Аудит фактического использования цветов

### Общая статистика

**Использование токенов:** ~60-70% интерфейса  
**Хардкоды:** ~30-40% интерфейса

### Детальная разбивка по категориям

#### ✅ UI (Интерфейс) — ~70% по токенам

**Используют токены:**
- Фоны страницы: `var(--Color/Dark/Background/Primary)` ✅
- Основные поверхности: `var(--Color/Dark/Surface/*)` ✅
- Текст в большинстве мест: `var(--Color/Light/Text/*)` ✅
- Акценты (частично): `var(--Color/Accent/Primary)` ✅
- Границы (частично): `var(--Color/Dark/Border/*)` ✅

**Хардкоды (требуют миграции):**
- **HUD заголовки:** `#CFE8FF` (используется в ~15 местах) — должен быть `var(--Color/Light/Text/Secondary)`
- **HUD текст:** `#9AA6B2` (используется в ~10 местах) — должен быть `var(--Color/Light/Text/Muted)`
- **Акценты hover:** `rgba(91,156,255,0.08)` (используется в ~20 местах) — должен быть `var(--Color/State/Hover/Background)`
- **Акценты active:** `rgba(91,156,255,0.15)` (используется в ~5 местах) — должен быть `var(--Color/State/Active/Background)`
- **Акцентные цвета:** `#5B9CFF`, `#4A8AE6` (используется в ~15 местах) — должны быть `var(--Color/Accent/Primary)`, `var(--Color/Accent/Hover)`
- **Границы:** `rgba(36,48,65,0.4)` (используется в ~8 местах) — должен быть `var(--Color/Dark/Border/Subtle)`

**Концентрация хардкодов:**
- `index.html` (CSS блок): ~50+ прямых hex/rgba значений
- HUD-стили: ~30 хардкодов
- Тур-стили: ~10 хардкодов

#### ⚠️ HUD/Тур — ~50% по токенам

**Используют токены:**
- Фоны панелей: `var(--hud-panel-bg)`, `var(--tour-card-bg)` ✅
- Бэкдропы: `var(--hud-backdrop)`, `var(--tour-backdrop)` ✅
- Основной текст (частично): `var(--hud-text)`, `var(--tour-text)` ✅

**Хардкоды (требуют миграции):**
- **HUD заголовки:** `#CFE8FF` (множественные использования) — должен быть `var(--Color/Light/Text/Secondary)` или `var(--hud-text)`
- **HUD вторичный текст:** `#9AA6B2` — должен быть `var(--Color/Light/Text/Muted)` или `var(--hud-text-muted)`
- **HUD ссылки:** `#5B9CFF` — должен быть `var(--Color/Accent/Primary)`
- **Тур ссылки:** `#9AA6B2`, `#5B9CFF` — должны использовать токены
- **Тени:** `rgba(0,0,0,0.25)`, `rgba(0,0,0,0.45)` — должны быть `var(--Color/Effect/Shadow/Soft)`, `var(--Color/Effect/Shadow/Medium)`

**Концентрация хардкодов:**
- HUD-панели: ~25 хардкодов
- Тур-карточки: ~8 хардкодов

#### ✅ 3D-сцена — ~95% по токенам

**Используют токены:**
- Все цвета сцены: `getSceneColors()` из `core/theme-colors.js` ✅
- Фон сцены: `sceneColors.background` ✅
- Лучи, эффекты, освещение: все через `getSceneColors()` ✅
- Палитра узлов: `sceneColors.nodePalette` ✅

**Хардкоды (минимальные):**
- **Grid overlay в engine.js:** `rgba(92, 182, 255, 0.18)` (динамически вычисляется) — можно мигрировать на `getSceneColors().sceneGrid`
- **Grid glow:** `rgba(92, 182, 255, 0.22)` — можно мигрировать на `getSceneColors().sceneAccent`

**Концентрация хардкодов:**
- `core/engine.js` (grid overlay): ~3-4 хардкода (динамические значения)

### Классификация найденных цветов

#### 1. Совпадает с токеном из системы тем (~60%)

**Примеры:**
- `#0B0F14` → `--Color/Dark/Background/Primary` (используется в critical-* переменных)
- `#58A6FF` → `--Color/Accent/Primary` (используется в theme-colors.js)
- `#F0F6FC` → `--Color/Light/Text/Primary` (используется в theme-colors.js)

**Проблема:** Цвета совпадают по значению, но используются напрямую вместо токенов.

#### 2. Похож по значению, но не привязан к токену (~25%)

**Примеры:**
- `#CFE8FF` (HUD заголовки) — похож на `--Color/Light/Text/Secondary` (`#D4E4F7`), но не идентичен
- `#9AA6B2` (HUD текст) — похож на `--Color/Light/Text/Muted` (`#8B98A5`), но не идентичен
- `rgba(91,156,255,0.08)` — похож на `--Color/State/Hover/Background` (`rgba(88, 166, 255, 0.10)`), но не идентичен

**Проблема:** Цвета близки к токенам, но имеют небольшие отличия. Требуется либо миграция на токены, либо создание новых токенов.

#### 3. Жёсткий хардкод вне системы (~15%)

**Примеры:**
- `rgba(36,48,65,0.4)` — специфичные границы, не привязаны к токенам
- `rgba(59, 74, 95, 0.9)` — специфичные фоны, не привязаны к токенам
- `#A5B4C3` — специфичный цвет текста, не привязан к токенам

**Проблема:** Цвета не имеют аналогов в системе токенов. Требуется либо создание новых токенов, либо миграция на существующие.

### Приоритеты миграции

#### Высокий приоритет (критично для консистентности)

1. **HUD заголовки и текст** (`#CFE8FF`, `#9AA6B2`) — ~25 использований
2. **Акцентные цвета** (`#5B9CFF`, `#4A8AE6`) — ~15 использований
3. **Состояния hover/active** (`rgba(91,156,255,0.08)`, `rgba(91,156,255,0.15)`) — ~25 использований

#### Средний приоритет (улучшение консистентности)

4. **Границы** (`rgba(36,48,65,0.4)`) — ~8 использований
5. **Тени** (`rgba(0,0,0,0.25)`, `rgba(0,0,0,0.45)`) — ~10 использований
6. **Тур-стили** — ~8 использований

#### Низкий приоритет (оптимизация)

7. **Grid overlay в engine.js** — ~3-4 использования (динамические значения)
8. **Критические inline-стили** — допустимы для предотвращения FOUC

### Рекомендации по миграции

1. **Создать недостающие токены** для цветов, которые похожи, но не идентичны существующим
2. **Заменить прямые значения** на `var(--token-name)` в CSS
3. **Использовать `getThemeColors()`** в JavaScript вместо прямых hex-значений
4. **Добавить fallback-значения** для критических inline-стилей (уже частично сделано)

### Метрики прогресса

- **UI:** 70% → цель 95%
- **HUD/Тур:** 50% → цель 90%
- **3D-сцена:** 95% → цель 100%
- **Общий прогресс:** 60-70% → цель 90%+

---

## Документация токенов: статические vs динамические

### ✅ Статические токены

**Определение:** Токены, значения которых строго заданы темой и не изменяются во время выполнения.

**Список статических токенов:**

#### UI (Интерфейс)

**Фоны:**
- `bgBase`, `bgSecondary`, `bgTertiary`, `bgMuted` — все фоны страницы
- `surface1`, `surface2`, `surface3` — все поверхности
- `surfacePanel`, `surfacePanelMobile`, `surfaceOverlay` — все панели и оверлеи

**Текст:**
- `textPrimary`, `textSecondary`, `textTertiary`, `textMuted`, `textOnAccent` — все цвета текста

**Акценты:**
- `accentPrimary`, `accentPrimaryHover`, `accentPrimaryActive`, `accentPrimaryPressed` — все акцентные цвета

**Границы:**
- `borderBase`, `borderSubtle`, `borderAccent` — все цвета границ

**Состояния:**
- `stateHoverBackground`, `stateActiveBackground`, `stateSelectedBackground`, `stateHoverSurface` — все состояния интерактивных элементов

**Эффекты:**
- `effectGlow`, `effectShadowSoft`, `effectShadowMedium` — все эффекты

#### 3D-сцена (контролируются темой)

**Базовые элементы (статичные, не анимируются):**
- `sceneBg` — фон сцены (обновляется при смене темы)
- `sceneGrid` — цвет сетки (используется в grid overlay)
- `sceneSpace` — фоновое пространство (используется в лучах Space → Node)
- `sceneCore` — цвет центральной сферы (используется в лучах Node → Core)
- `sceneAccent` — базовый акцентный цвет сцены (используется как референс)

**Лучи (статичные цвета, прозрачность может изменяться):**
- `sceneBeamBase` — базовый цвет лучей
- `sceneBeamLine` — цвет линий лучей
- `sceneBeamInner` — внутренний цвет лучей
- `sceneBeamOuter` — внешний цвет лучей
- `sceneBeamA`, `sceneBeamB` — цвета для туманности (nebula)

**Звёзды и туман (статичные цвета, позиции анимируются):**
- `sceneStarNear` — цвет ближних звёзд
- `sceneStarFar` — цвет дальних звёзд
- `sceneMist` — цвет тумана (используется в фоновом тумане)

**Освещение (статичное, не анимируется):**
- `sceneLightHemi` — цвет полусферического света (верх)
- `sceneLightHemiGround` — цвет земли полусферического света (низ)
- `sceneLightDir` — цвет направленного света

**Wireframe (статичный цвет, видимость контролируется анимацией):**
- `wire` — цвет каркаса центральной сферы (используется при импульсе)

**Использование:** Эти токены используются напрямую без модификаций:
```javascript
const colors = getThemeColors();
const bgColor = colors.bgBase; // Используется напрямую

// В 3D-сцене
const sceneColors = getSceneColors();
scene.background = toThreeColor(sceneColors.background); // Статический цвет
```

### 🔄 Динамические токены (база для динамики)

**Определение:** Токены, которые используются как база для динамических эффектов, но сами остаются статическими. Значение токена задаётся темой, но используется в динамических вычислениях (прозрачность, интенсивность, яркость).

**Список динамических токенов:**

#### 3D-сцена

**Импульсы и вспышки:**
- `impulseColor` — используется как база для импульсов при взаимодействии с узлами
  - **Контролируется темой:** ✅ Да (значение задаётся темой)
  - **Контролируется анимацией:** ✅ Да (прозрачность, интенсивность, позиция пакета)
  - **Использование:** 
    ```javascript
    // В шейдерах лучей
    packetColor: { value: new THREE.Color(impulseColor) }
    // Прозрачность и позиция пакета изменяются динамически
    ```
  - **Где используется:** Лучи Space → Node, Node → Space (бегущие пакеты данных)

- `scenePulse` — используется как база для пульсаций центральной сферы
  - **Контролируется темой:** ✅ Да (значение задаётся темой)
  - **Контролируется анимацией:** ✅ Да (интенсивность, прозрачность, частота)
  - **Использование:**
    ```javascript
    // В шейдерах stream rings
    color: { value: toThreeColor(scenePulse) }
    // Интенсивность изменяется: pulseIntensity * 0.6
    ```
  - **Где используется:** Stream rings (электрический голографический эффект вокруг центра)

- `scenePulseB` — используется как база для пульсаций типа B
  - **Контролируется темой:** ✅ Да (значение задаётся темой)
  - **Контролируется анимацией:** ✅ Да (смешивание с `scenePulse`, интенсивность)
  - **Использование:**
    ```javascript
    // Смешивание цветов в шейдере
    vec3 baseCol = mix(color, colorB, colorMix * 0.7);
    ```
  - **Где используется:** Stream rings (дополнительный цвет для вариативности)

**Лучи (база для динамических эффектов):**
- `sceneBeamBase`, `sceneBeamInner`, `sceneBeamOuter` — используются как база для лучей
  - **Контролируется темой:** ✅ Да (значение задаётся темой)
  - **Контролируется анимацией:** ✅ Да (прозрачность, позиция пакета, интенсивность свечения)
  - **Использование:**
    ```javascript
    // В шейдерах лучей
    colorFrom: { value: new THREE.Color(beamBase) }
    colorTo: { value: new THREE.Color(beamInner) }
    // Прозрачность и позиция пакета изменяются динамически
    ```
  - **Где используется:** Лучи между узлами, Space, Core (бегущие пакеты данных)

**Примечание:** Хотя эти токены статические по значению, они предназначены для использования в динамических вычислениях. При смене темы значения токенов обновляются, но логика анимации (прозрачность, интенсивность, позиция) остаётся неизменной.

### 📊 Динамический выбор из статической палитры

**Определение:** Токены, которые представляют палитру статических цветов, из которых выбирается цвет динамически.

**Список палитр:**

- `nodePalette` — массив из 8 статических цветов для узлов
  - **Контролируется темой:** ✅ Да (все цвета заданы темой)
  - **Контролируется анимацией:** ❌ Нет (выбор цвета статичен на основе индекса узла)
  - **Использование:** 
    ```javascript
    // Выбор цвета на основе индекса узла
    const NODE_COLOR = NODE_PALETTE[idx % NODE_PALETTE.length];
    ```
  - **Где используется:** Цвета узлов (сферы), цвета лучей Node → Core, Core → Node
  - **Примечание:** Палитра статическая (все цвета заданы темой), но выбор цвета из палитры происходит динамически на основе индекса узла. При смене темы все цвета палитры обновляются, но индексы узлов остаются неизменными.

---

## Контроль параметров 3D-сцены: тема vs анимация

### ✅ Контролируются темой (статичные значения)

**При смене темы (Light/Dark) обновляются:**

1. **Фон сцены** (`sceneBg`)
   - Обновляется через `scene.background = toThreeColor(sceneColors.background)`
   - Где: `core/engine.js`, `ui/theme-controller.js`

2. **Освещение** (`sceneLightHemi`, `sceneLightHemiGround`, `sceneLightDir`)
   - Обновляется через `hemi.color = new THREE.Color(sceneColors.lightHemi)`
   - Где: `ui/theme-controller.js` (при смене темы), `scenes/calmScene-complete.js` (при монтировании)

3. **Все статические цвета** (звёзды, туман, лучи, узлы)
   - Обновляются при монтировании сцены через `getSceneColors()`
   - Где: `scenes/calmScene-complete.js` (в функции `mount()`)

**Важно:** Статические цвета читаются один раз при монтировании сцены. Для обновления при смене темы требуется перемонтирование сцены или явное обновление материалов.

### 🔄 Контролируются анимацией (динамические значения)

**Во время выполнения изменяются:**

1. **Прозрачность** (`opacity`, `alpha`)
   - Изменяется в шейдерах на основе времени, позиции пакета, интенсивности
   - Пример: `float a = radial * (glow + packet * (1.0 - glow));`

2. **Интенсивность** (`intensity`, `brightness`)
   - Изменяется в шейдерах на основе пульсаций, импульсов, мигания
   - Пример: `float pulse = smoothstep(0.15, 0.0, abs(head-0.5));`

3. **Позиция пакета** (`packetPos`)
   - Изменяется в шейдерах для создания эффекта бегущего пакета данных
   - Пример: `float dist = abs(vUv.y - packetPos);`

4. **Смешивание цветов** (`colorMix`)
   - Изменяется в шейдерах для создания плавных переходов
   - Пример: `vec3 baseCol = mix(color, colorB, colorMix * 0.7);`

5. **Видимость элементов** (`visible`, `opacity`)
   - Изменяется в JavaScript для показа/скрытия элементов (например, wireframe при импульсе)
   - Пример: `coreWire.visible = true; coreWireMat.opacity = 1.0;`

**Важно:** Динамические эффекты используют статические токены как базу, но модифицируют их во время выполнения. При смене темы база обновляется, но логика анимации остаётся неизменной.

### 🔄 Синхронизация смены темы и обновления сцены

**Механизм обновления:**

1. **При монтировании сцены:**
   ```javascript
   // scenes/calmScene-complete.js
   const currentSceneColors = getSceneColors(); // Читает текущую тему
   scene.background = toThreeColor(currentSceneColors.background);
   ```

2. **При смене темы:**
   ```javascript
   // ui/theme-controller.js
   const sceneColors = getSceneColors(); // Читает новую тему
   scene.background = new THREE.Color(sceneColors.background);
   // Обновление освещения
   hemi.color = new THREE.Color(sceneColors.lightHemi);
   ```

3. **Для полного обновления материалов:**
   - Требуется перемонтирование сцены или явное обновление всех материалов
   - В текущей реализации материалы обновляются при монтировании, но не при смене темы
   - **Рекомендация:** Добавить метод `updateTheme()` в сцену для обновления всех материалов без перемонтирования

**Примечание:** В текущей реализации статические цвета обновляются при монтировании сцены и при смене темы (фон, освещение). Динамические эффекты продолжают работать с новыми базовыми цветами, но логика анимации остаётся неизменной.

---

## Соответствие с `core/theme-colors.js`

### Структура объектов тем

**`darkTheme` и `lightTheme`** структурированы одинаково и соответствуют разделам COLOR_MAP.md:

1. **Базовая палитра** (раздел 1)
   - Нейтральные цвета
   - Акцентные цвета
   - Статусные цвета (⚠️ требуется добавление)

2. **Семантические роли интерфейса** (раздел 2)
   - Фон страницы
   - Поверхности
   - Текст
   - Интерактив
   - Состояния (⚠️ требуется добавление)
   - Границы
   - Эффекты

3. **3D-пакет** (раздел 3)
   - Фон сцены
   - Сетка/Орбиты
   - Базовая сфера
   - Диапазон для динамических эффектов

### Использование в коде

```javascript
// Получить цвета текущей темы
import { getThemeColors, getSceneColors } from './core/theme-colors.js';

// UI цвета
const uiColors = getThemeColors();
const bgColor = uiColors.bgBase; // ✅ Статический токен
const textColor = uiColors.textPrimary; // ✅ Статический токен

// 3D-сцена цвета
const sceneColors = getSceneColors();
const sceneBg = sceneColors.background; // ✅ Статический токен
const impulseColor = sceneColors.impulseColor; // 🔄 База для динамики

// Динамическое использование
const dynamicImpulse = `rgba(${impulseColor}, ${alpha})`; // Прозрачность изменяется
const nodeColor = sceneColors.nodePalette[nodeIndex % sceneColors.nodePalette.length]; // 📊 Выбор из палитры
```

### Маркировка в коде

В `core/theme-colors.js` все токены помечены комментариями:
- `// ✅ Статический` — токен используется напрямую
- `// 🔄 База для динамики` — токен используется как база для динамических вычислений
- `// 📊 Динамический выбор` — палитра статических цветов для динамического выбора

---

## Правило изменений: как работать с цветами

### ⚠️ Критическое правило

> **Любые новые цвета → сначала в `docs/COLOR_MAP.md`, затем в `core/theme-colors.js`**

**Почему:**
- Предотвращает появление "двойной правды" (разные документы с разными цветами)
- Гарантирует, что все цвета задокументированы
- Обеспечивает единый источник истины

### Технические правила

#### ❌ Запрещено

1. **Добавлять новые hex/rgb/hsl в UI/3D без привязки к токену**
   ```javascript
   // ❌ НЕ ДЕЛАЙТЕ ТАК
   element.style.color = '#5B9CFF';
   element.style.background = 'rgba(91, 156, 255, 0.08)';
   ```

2. **Создавать новые документы про цвета**
   - Не создавайте `COLORS.md`, `THEME-COLORS.md`, `PALETTE.md` и т.п.
   - Вся документация по цветам должна быть в `docs/COLOR_MAP.md`

3. **Дублировать информацию о цветах в других файлах**
   - Не копируйте таблицы цветов в README, ARCHITECTURE и т.п.
   - Используйте ссылки на `docs/COLOR_MAP.md`

#### ✅ Разрешено

1. **Использовать прямые цвета только в `core/theme-colors.js`**
   ```javascript
   // ✅ МОЖНО в theme-colors.js
   const PALETTE = {
     accentPrimary: '#58A6FF', // Прямой цвет допустим
   };
   ```

2. **Использовать токены везде**
   ```javascript
   // ✅ В JS
   const colors = getThemeColors();
   element.style.color = colors.accentPrimary;
   
   // ✅ В CSS
   .my-element {
     color: var(--Color/Accent/Primary);
   }
   ```

### Процесс добавления нового цвета

#### Шаг 1: Документирование в COLOR_MAP.md

1. Определите семантическую роль цвета:
   - Фон? Текст? Акцент? Граница? Состояние?
   - UI или 3D-сцена?

2. Найдите подходящий раздел в `docs/COLOR_MAP.md`:
   - Базовая палитра (1.1, 1.2, 1.3)
   - Семантические роли интерфейса (2.1-2.5)
   - 3D-пакет (3.1-3.4)

3. Добавьте токен в таблицу с описанием:
   ```markdown
   | Токен (JS) | Dark Theme | Light Theme | Роль | Статический/Динамический |
   |------------|------------|-------------|------|--------------------------|
   | `newToken` | `#XXXXXX` | `#YYYYYY` | Описание роли | ✅ Статический |
   ```

4. Определите тип токена:
   - ✅ Статический — используется напрямую
   - 🔄 База для динамики — используется как база для вычислений
   - 📊 Динамический выбор — палитра для выбора

#### Шаг 2: Реализация в theme-colors.js

1. Добавьте цвет в `PALETTE` (если нужен базовый цвет):
   ```javascript
   const PALETTE = {
     // ... существующие цвета
     newColor: '#XXXXXX', // Новый базовый цвет
   };
   ```

2. Добавьте токен в `darkTheme` и `lightTheme`:
   ```javascript
   const darkTheme = {
     // ... существующие токены
     newToken: PALETTE.newColor, // ✅ Статический
   };
   
   const lightTheme = {
     // ... существующие токены
     newToken: PALETTE.newColorLight, // ✅ Статический
   };
   ```

3. Добавьте комментарий с типом токена:
   ```javascript
   newToken: PALETTE.newColor, // ✅ Статический
   // или
   newToken: PALETTE.newColor, // 🔄 База для динамики
   ```

#### Шаг 3: Добавление CSS-переменной (если нужно для UI)

1. Добавьте переменную в `styles/tokens.css`:
   ```css
   :root {
     --Color/New/Token: #XXXXXX;
   }
   
   :root.theme-light {
     --Color/New/Token: #YYYYYY;
   }
   ```

2. Используйте в CSS:
   ```css
   .my-element {
     color: var(--Color/New/Token);
   }
   ```

#### Шаг 4: Использование в коде

**В JavaScript:**
```javascript
import { getThemeColors, getSceneColors } from './core/theme-colors.js';

// UI цвета
const uiColors = getThemeColors();
const newColor = uiColors.newToken;

// 3D-сцена цвета
const sceneColors = getSceneColors();
const sceneNewColor = sceneColors.newToken;
```

**В CSS:**
```css
.my-element {
  color: var(--Color/New/Token);
}
```

**В HTML (inline styles):**
```html
<div style="color: var(--Color/New/Token);">
```

### Обновление существующего цвета

1. **Обновите значение в `core/theme-colors.js`** (в `PALETTE` и/или темах)
2. **Обновите значение в `styles/tokens.css`** (если используется CSS-переменная)
3. **Обновите документацию в `docs/COLOR_MAP.md`** (таблица значений)

**Важно:** Все три места должны быть синхронизированы!

### Проверка перед коммитом

Перед коммитом убедитесь:

1. ✅ Новый цвет добавлен в `docs/COLOR_MAP.md`
2. ✅ Новый цвет добавлен в `core/theme-colors.js`
3. ✅ CSS-переменная добавлена в `styles/tokens.css` (если нужна)
4. ✅ Нет прямых hex/rgb/hsl в коде (кроме `theme-colors.js`)
5. ✅ Документация синхронизирована с кодом

**Проверка хардкодов:**
```bash
# Найти все жесткие цвета в JS (кроме theme-colors.js)
grep -r "#[0-9A-Fa-f]\{3,6\}" --include="*.js" --exclude="theme-colors.js" .

# Найти все жесткие цвета в CSS (кроме tokens.css)
grep -r "#[0-9A-Fa-f]\{3,6\}" --include="*.css" --exclude="tokens.css" .
```

---

## Руководство для разработчиков

### Как добавить новый цвет/роль

**Пример:** Нужен новый цвет для предупреждений (warning).

1. **Документирование:**
   - Откройте `docs/COLOR_MAP.md`
   - Найдите раздел "1.3 Статусные цвета" (или создайте, если нет)
   - Добавьте токен в таблицу:
     ```markdown
     | `statusWarning` | `#F59E0B` | `#F59E0B` | Цвет предупреждений | ✅ Статический |
     ```

2. **Реализация:**
   - Откройте `core/theme-colors.js`
   - Добавьте в `PALETTE`:
     ```javascript
     statusWarning: '#F59E0B',
     ```
   - Добавьте в `darkTheme` и `lightTheme`:
     ```javascript
     statusWarning: PALETTE.statusWarning, // ✅ Статический
     ```

3. **CSS-переменная (если нужна):**
   - Откройте `styles/tokens.css`
   - Добавьте:
     ```css
     --Color/Status/Warning: #F59E0B;
     ```

4. **Использование:**
   ```javascript
   // В JS
   const colors = getThemeColors();
   warningElement.style.color = colors.statusWarning;
   
   // В CSS
   .warning {
     color: var(--Color/Status/Warning);
   }
   ```

### Как обновить тему

**Пример:** Нужно изменить акцентный цвет.

1. **Обновите `core/theme-colors.js`:**
   ```javascript
   const PALETTE = {
     accentPrimary: '#NEW_COLOR', // Изменить здесь
   };
   ```

2. **Обновите `styles/tokens.css`:**
   ```css
   :root {
     --Color/Accent/Primary: #NEW_COLOR;
   }
   ```

3. **Обновите `docs/COLOR_MAP.md`:**
   - Обновите значение в таблице

**Важно:** Все три места должны быть синхронизированы!

### Как использовать токен в UI/3D

**В UI (CSS):**
```css
.my-button {
  background: var(--Color/Accent/Primary);
  color: var(--Color/Light/Text/OnBrand);
}
```

**В UI (JavaScript):**
```javascript
import { getThemeColors } from './core/theme-colors.js';

const colors = getThemeColors();
element.style.backgroundColor = colors.accentPrimary;
```

**В 3D-сцене:**
```javascript
import { getSceneColors, toThreeColor } from './core/theme-colors.js';

const sceneColors = getSceneColors();
scene.background = toThreeColor(sceneColors.background);
```

### Как не нужно делать

**❌ Не создавайте локальные темы:**
```javascript
// ❌ НЕ ДЕЛАЙТЕ ТАК
const localTheme = {
  primary: '#5B9CFF',
  secondary: '#4A8AE6',
};
```

**❌ Не используйте прямые цвета:**
```javascript
// ❌ НЕ ДЕЛАЙТЕ ТАК
element.style.color = '#5B9CFF';
```

**❌ Не дублируйте документацию:**
```markdown
<!-- ❌ НЕ ДЕЛАЙТЕ ТАК -->
# Цвета проекта
- Акцент: #5B9CFF
- Фон: #0D1117
```

**✅ Делайте так:**
```javascript
// ✅ ДЕЛАЙТЕ ТАК
import { getThemeColors } from './core/theme-colors.js';
const colors = getThemeColors();
element.style.color = colors.accentPrimary;
```

```markdown
<!-- ✅ ДЕЛАЙТЕ ТАК -->
См. [документацию по цветам](docs/COLOR_MAP.md)
```

---

## Code Review Checklist

При code review проверяйте:

- [ ] Новые цвета добавлены в `docs/COLOR_MAP.md`
- [ ] Новые цвета добавлены в `core/theme-colors.js`
- [ ] CSS-переменные добавлены в `styles/tokens.css` (если нужны)
- [ ] Нет прямых hex/rgb/hsl в коде (кроме `theme-colors.js`)
- [ ] Документация синхронизирована с кодом
- [ ] Тип токена указан правильно (✅/🔄/📊)

---

## FAQ

**Q: Можно ли использовать прямые цвета в критических inline-стилях?**  
A: Да, но только с fallback на токены:
```html
<div style="background: var(--Color/Dark/Background/Primary, #0D1117);">
```

**Q: Где хранятся цвета для 3D-сцены?**  
A: В `core/theme-colors.js` в объектах `darkTheme` и `lightTheme` (раздел 3. 3D-пакет).

**Q: Как добавить новый цвет для тура?**  
A: Добавьте токен в раздел "2. Семантические роли интерфейса" → "2.5 Состояния" или создайте новый раздел для тура.

**Q: Можно ли создать локальную тему для компонента?**  
A: Нет. Все цвета должны быть в `core/theme-colors.js` и задокументированы в `docs/COLOR_MAP.md`.

**Q: Что делать, если цвет похож на существующий, но не идентичен?**  
A: Либо используйте существующий токен, либо создайте новый токен с явным описанием различий в `docs/COLOR_MAP.md`.

