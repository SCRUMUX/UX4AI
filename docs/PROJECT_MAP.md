# Карта проекта UX4AI

**Версия:** Baseline (dev-ветка, стабильное состояние)  
**Дата создания:** 2024  
**Статус:** Фаза 4 — финализация и контроль качества

---

## 📁 Структура директорий

```
UX4AI/
├── core/                    # Ядро 3D-движка и базовые системы
├── ui/                      # UI компоненты и контроллеры
├── scenes/                  # 3D-сцены (плагины)
├── themes/                  # Конфигурации тем и сцен
├── data/                    # Данные контента (узлы, секции, ссылки)
├── styles/                  # CSS стили и дизайн-токены
├── docs/                    # Документация
├── archive/                 # Архив неиспользуемых файлов (Фаза 5)
├── index.html              # Главный HTML файл (монолитный)
├── sw.js                    # Service Worker для кэширования
├── ux4ai-preloader-loop-outline-noise.html  # Прелоадер (анимация)
├── Vladimir_Kostyal_Resume.pdf  # Резюме (статический контент)
└── [другие файлы]          # Статические ресурсы, изображения, SVG
```

---

## 🎯 Основные директории и их назначение

### `core/` — Ядро 3D-движка

**Назначение:** Базовые системы для работы с Three.js, управление состоянием, взаимодействие с 3D-объектами.

**Ключевые файлы:**

- **`engine.js`** — Главный движок: создание и управление Three.js renderer, camera, scene, анимационный цикл. Монтирование/размонтирование сцен, управление рендерингом (пауза на мобильных в туре), интеграция с navigation и HUD.

- **`state.js`** — Глобальное состояние и простой event bus. Хранит `activeThemeId`, `activeSceneId`, `activeSection`, `orbitModeEnabled`. Предоставляет функции `on()`, `emit()`, `set()`, `get()` для событий и состояния.

- **`navigation.js`** — Управление навигацией камеры: скролл-навигация, орбитальный режим, синхронизация с прокруткой страницы. Инициализируется после монтирования сцены.

- **`picking.js`** — Raycasting для взаимодействия с 3D-объектами. Обработка кликов по узлам сцены, эмиссия события `nodeSelected` для HUD.

- **`labels.js`** — Управление 2D-метками поверх 3D-сцены. Позиционирование меток относительно камеры, обновление позиций в анимационном цикле.

- **`sections.js`** — Валидация и маппинг секций. Содержит `SECTION_NAMES`, `getSectionId()`, `validateAnchors()` для связи 3D-узлов с контентом.

- **`hud-manager.js`** — Менеджер HUD-панелей: отображение панелей с контентом узлов, навигация между секциями, обработка кнопок (Демо, Автор, Ссылки), создание и управление кнопкой переключения темы. Инициализируется после монтирования сцены.

- **`theme-colors.js`** — Централизованная система цветов для тем. Определяет палитру и семантические токены для dark/light тем (UI и 3D-сцена). Экспортирует `getSceneColors()`, `toThreeColor()` для использования в сценах.

- **`tour.js`** — Модуль тура (Фаза 3): конфигурация шагов, функции управления туром (`startTour()`, `setStep()`, `showTour()`, `hideTour()`, `tour_reset()`). Вся логика тура вынесена из `index.html` в отдельный модуль.

---

### `ui/` — UI компоненты и контроллеры

**Назначение:** UI-логика для переключения тем, инициализация интерфейса.

**Ключевые файлы:**

- **`theme-controller.js`** — Центральный контроллер тем. Единственная точка применения темы: обновляет CSS-классы, CSS-переменные, фон 3D-сцены, сохраняет в localStorage. Экспортирует `applyThemeById()`, `getCurrentThemeId()`, `initTheme()`.

- **`themeSwitcher.js`** — UI-обертка для переключения тем. Инициализирует тему при старте, монтирует сцену один раз. Использует `theme-controller.js` для применения тем. Создает кнопку переключения темы (логика в `hud-manager.js`).

---

### `scenes/` — 3D-сцены (плагины)

**Назначение:** Плагины 3D-сцен, которые монтируются в движок.

**Ключевые файлы:**

- **`calmScene-complete.js`** — Единственная активная 3D-сцена "Calm". Создает узлы, лучи, голограммы, эффекты. Экспортирует factory-функцию `calmSceneCompleteFactory()`, которая возвращает объект с методами `mount()`, `update()`, `dispose()`, `getLabelAnchors()`, `getActiveCamera()`. Использует цвета из `theme-colors.js`.

---

### `themes/` — Конфигурации тем и сцен

**Назначение:** Конфигурации для тем UI и 3D-сцен.

**Ключевые файлы:**

- **`calm.js`** — Конфигурация сцены "Calm". Определяет `id`, `name`, `sceneFactory` (ссылка на `calmSceneCompleteFactory`), `config` (палитра узлов, скорости). Не содержит UI-тему.

- **`index.js`** — Реестр сцен. Экспортирует `SCENES` (содержит только `calm` — единственная активная сцена), функции `getScene()`, `getAllSceneIds()`. Предоставляет legacy-экспорты `THEMES`, `getTheme()` для обратной совместимости.

- **`ui-theme-config.js`** — Конфигурации UI-тем (dark/light). Определяет `DARK_THEME_CONFIG` и `LIGHT_THEME_CONFIG` с CSS-классами, meta-цветами, используется `theme-controller.js`.

- **`legacy/theme-variables.css`** ⚠️ **LEGACY** — Старые CSS-переменные для тем (в `themes/legacy/`). Содержит переменные `--bg-primary`, `--text-primary` и т.д. Не используется, готов к удалению.

- **`legacy/simple-themes.css`** ⚠️ **LEGACY** — Простые темы (light, neon) (в `themes/legacy/`). Содержит CSS-переменные для дополнительных тем. Не используется, готов к удалению.

---

### `data/` — Данные контента

**Назначение:** Контентные данные для узлов, секций, ссылок.

**Ключевые файлы:**

- **`nodes-data-complete.js`** — Данные узлов 3D-сцены. Экспортирует `NODES_DATA` (контент для каждого узла: summary, problem, solution, ui, figma), `ABOUT_DATA` (данные для узла "Автор"), `LINKS` (ссылки на Telegram, резюме и т.д.).

---

### `archive/` — Архив неиспользуемых файлов

**Назначение:** Неиспользуемые файлы, перенесённые из корня проекта.

**Статус:** ✅ **Создан в Фазе 5** — Кодовая чистка

**Содержимое:**
- `cyber_materials.js` — материалы для Three.js (экспериментальная неоновая тема, см. `docs/legacy/README-NEON-CUBES.md`)
- `Cfmerapath.svg` — неиспользуемая SVG иконка
- `wallpaper_24dp_434343_FILL0_wght400_GRAD0_opsz24.svg` — неиспользуемая SVG иконка
- `Matrix.glb` — 3D модель для удалённой Matrix-сцены
- `path.glb` — неиспользуемая 3D модель
- `crosshair-blue.ani` — неиспользуемый анимационный файл

**Подробнее:** см. [`archive/README.md`](../archive/README.md)

---

### ~~`utils/`~~ — Удалён (Фаза 5)

**Статус:** ❌ **Удалён в Фазе 5** — каталог был пуст после переноса `cyber_materials.js` в `archive/`

---

### `styles/` — CSS стили

**Назначение:** Стили и дизайн-токены.

**Статус:** ✅ **Каталог существует** и содержит актуальные файлы.

**Ключевые файлы:**

- **`tokens.css`** — Дизайн-токены Synaptik Design System. Определяет CSS-переменные для цветов, типографики, отступов, z-index, переходов. Содержит переопределения для light-темы через `:root.theme-light`. **Используется в `index.html`.**

- **`main.css`** — Основные стили. Использует токены из `tokens.css`. Стили для header, кнопок, HUD-панелей, тура, overlay, адаптивные стили для мобильных. **Используется в `index.html`.**

- **`variables.css`** ⚠️ **LEGACY** — Тонкий alias-слой для обратной совместимости. Все переменные маппятся на токены из `tokens.css`. Кандидат на удаление после полной миграции.

---

### Корневой уровень

**Ключевые файлы:**

- **`index.html`** — Монолитный HTML-файл. Содержит разметку, inline CSS для критических стилей, JavaScript для тура, bootstrap-скрипт для инициализации (импорт модулей, создание engine, инициализация темы, монтирование сцены, инициализация HUD). Логика тура встроена в HTML.

- **`sw.js`** — Service Worker для кэширования. Версионирование кэша через `BUILD_VERSION`, кэширование статических ресурсов.

- **`ux4ai-preloader-loop-outline-noise.html`** — Прелоадер (анимация). Используется в `index.html` через `<iframe>` в элементе `#preloader-overlay`.

- **`Vladimir_Kostyal_Resume.pdf`** — Резюме (статический контент). Используется в HUD-панели "Автор" как ссылка для скачивания.

**⚠️ Проблема согласованности:**

- **`/assets/UX4AI_90day_plan.pdf`** — `index.html` ссылается на этот файл (строки 3492, 3513), но файл и каталог `assets/` не существуют в проекте. Требуется либо создать файл/каталог, либо удалить ссылки.

---

## 🔧 Группировка по подсистемам

### 🎮 Ядро 3D-движка

**Файлы:**
- `core/engine.js` — главный движок
- `core/navigation.js` — навигация камеры
- `core/picking.js` — raycasting
- `core/labels.js` — 2D-метки
- `core/sections.js` — валидация секций
- `core/state.js` — глобальное состояние
- `scenes/calmScene-complete.js` — плагин сцены

**Ответственность:** Инициализация Three.js, рендеринг, взаимодействие с 3D-объектами, управление камерой, анимационный цикл.

---

### 🖥️ UI/HUD

**Файлы:**
- `core/hud-manager.js` — менеджер HUD-панелей
- `styles/main.css` — стили UI
- `styles/tokens.css` — дизайн-токены
- `index.html` (частично) — разметка HUD, inline стили

**Ответственность:** Отображение панелей с контентом, навигация между секциями, кнопки интерфейса, стилизация.

---

### 🎨 Темы

**Файлы:**
- `ui/theme-controller.js` — контроллер тем
- `ui/themeSwitcher.js` — UI-обертка
- `core/theme-colors.js` — система цветов
- `themes/ui-theme-config.js` — конфигурации UI-тем
- `themes/calm.js` — конфигурация сцены
- `themes/index.js` — реестр сцен
- `styles/tokens.css` — CSS-переменные тем

**Ответственность:** Переключение dark/light тем, применение цветов к UI и 3D-сцене, сохранение выбора темы.

---

### 🧭 Тур и Демо

**Файлы:**
- `index.html` (встроенная логика) — весь функционал тура
- `core/hud-manager.js` (кнопка "Демо") — открытие HUD с секцией "Основы UX для AI"

**Ответственность:** Отображение пошагового тура, управление шагами, сохранение прогресса, кнопка перезапуска тура, кнопка "Демо" для быстрого доступа к контенту.

---

### 🛠️ Вспомогательные утилиты

**Файлы:**
- `data/nodes-data-complete.js` — данные контента
- `sw.js` — Service Worker
- `index.html` (bootstrap) — инициализация приложения
- `ux4ai-preloader-loop-outline-noise.html` — прелоадер (анимация)
- `Vladimir_Kostyal_Resume.pdf` — резюме (статический контент)

**Ответственность:** Хранение данных, кэширование, точка входа приложения, вспомогательные ресурсы.

**Примечание:** Неиспользуемые файлы перенесены в `archive/` (Фаза 5).

---

## ⚠️ Подозрительные/устаревшие части (кандидаты на аудит)

### 🔴 Высокий приоритет аудита

1. **`themes/legacy/theme-variables.css`**
   - Старые CSS-переменные (`--bg-primary`, `--text-primary` и т.д.)
   - Дублирует функциональность `styles/tokens.css`
   - **Статус:** Не используется, находится в `themes/legacy/`, готов к удалению

2. **`themes/legacy/simple-themes.css`**
   - Содержит определения для light и neon тем
   - **Статус:** Не используется, находится в `themes/legacy/`, готов к удалению

### 🟡 Средний приоритет аудита

4. **Монолитный `index.html`**
   - Содержит большой объем inline JavaScript (логика тура, bootstrap)
   - Смешивает разметку, стили и логику
   - **Действие:** Рассмотреть вынос логики тура в отдельный модуль

5. **Дублирование логики создания кнопок**
   - Кнопка темы создается в `hud-manager.js`
   - Кнопка tour-restart создается в `index.html` (bootstrap)
   - **Действие:** Унифицировать создание кнопок header

6. **Несколько MutationObserver**
   - В `index.html` есть MutationObserver для tour-restart
   - В `core/hud-manager.js` есть MutationObserver для защиты кнопки темы
   - Возможны конфликты
   - **Действие:** Проверить взаимодействие observers

### 🟢 Низкий приоритет аудита

7. **Версионирование модулей через query params**
   - Многие импорты имеют `?v=XX` (например, `./core/engine.js?v=37`)
   - Используется для cache busting
   - **Действие:** Рассмотреть автоматизацию версионирования

8. **Документация**
   - Есть `THEME-ARCHITECTURE.md` и `THEME-SYSTEM.md`
   - Неясно, актуальна ли документация
   - **Действие:** Проверить актуальность и дополнить при необходимости

---

## 📊 Зависимости между модулями

### Граф зависимостей (упрощенный)

```
index.html (bootstrap)
  ├── core/engine.js
  │     ├── core/picking.js
  │     ├── core/labels.js
  │     ├── core/sections.js
  │     ├── core/state.js
  │     ├── core/navigation.js
  │     └── core/hud-manager.js
  │           ├── data/nodes-data-complete.js
  │           ├── core/sections.js
  │           └── core/state.js
  ├── ui/themeSwitcher.js
  │     └── ui/theme-controller.js
  │           ├── core/state.js
  │           ├── themes/ui-theme-config.js
  │           └── core/theme-colors.js
  └── themes/calm.js
        └── scenes/calmScene-complete.js
              ├── core/sections.js
              └── core/theme-colors.js
```

---

## 🎯 Точки входа

1. **`index.html`** — главная точка входа
   - Bootstrap-скрипт инициализирует приложение
   - Последовательность: импорт THREE → создание engine → инициализация темы → монтирование сцены → инициализация HUD

2. **`sw.js`** — Service Worker (регистрируется из `index.html`)

---

## 📝 Примечания

- **Текущее состояние:** dev-ветка в стабильном состоянии
- **Активная тема:** темная (по умолчанию), переключатель тем (Dark/Light)
- **Активная сцена:** только `calmScene-complete` (единственная 3D-сцена)
- **Кнопка темы:** создаётся в `core/hud-manager.js`
- **Тур:** модуль `core/tour.js` (Фаза 3), работает
- **Демо:** работает

---

## 🔍 Рекомендации для дальнейшего аудита

1. Рассмотреть удаление `themes/legacy/theme-variables.css` и `themes/legacy/simple-themes.css` (не используются)
2. Рассмотреть рефакторинг монолитного `index.html`
4. Унифицировать создание кнопок header
5. Проверить взаимодействие MutationObserver
6. Обновить/дополнить документацию

---

**Документ обновлён:** Фаза 2 — Структура архитектурных документов  
**Статус:** ✅ Приведён в соответствие с фактической структурой проекта (Фаза 1 — Полная инвентаризация)

---

## 📝 Изменения

### Фаза 2 — Структура архитектурных документов

**Добавлено:**
- Файлы корневого уровня: `ux4ai-preloader-loop-outline-noise.html`, `Vladimir_Kostyal_Resume.pdf`
- Уточнение статуса каталога `styles/` (существует, содержит актуальные файлы)
- Проблема согласованности: `/assets/UX4AI_90day_plan.pdf` (отсутствует)

**Обновлено:**
- Структура директорий приведена в соответствие с фактической
- Описание `styles/` уточнено (файлы существуют и используются)

### Фаза 5 — Кодовая чистка

**Добавлено:**
- Каталог `archive/` с неиспользуемыми файлами

**Удалено/Перенесено:**
- Каталог `utils/` — удалён (был пуст после переноса файлов)
- `utils/cyber_materials.js` → `archive/cyber_materials.js`
- Неиспользуемые медиа-файлы → `archive/`:
  - `Cfmerapath.svg`
  - `wallpaper_24dp_434343_FILL0_wght400_GRAD0_opsz24.svg`
  - `Matrix.glb`
  - `path.glb`
  - `crosshair - blue.ani` → `archive/crosshair-blue.ani`
- `tatus` — удалён (мусорный файл)

**Обновлено:**
- `sw.js` — удалён паттерн кеширования для `utils/`
- Структура директорий обновлена (удалён `utils/`, добавлен `archive/`)

