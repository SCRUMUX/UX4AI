# Целевая архитектура проекта UX4AI

**Версия:** Фаза 4 — финализация и контроль качества  
**Базовый baseline:** dev-ветка (стабильное состояние)  
**Дата создания:** 2024  
**Последнее обновление:** Фаза 4 — обновлено: модуль тура, одна сцена, актуальная структура

---

## Введение

### Назначение проекта

UX4AI — это интерактивный веб-сайт, демонстрирующий методологию UX для AI-продуктов через:

- **3D-сцену** (`calmScene-complete`): единственная активная 3D-сцена, интерактивная визуализация узлов знаний в трёхмерном пространстве
- **HUD (Head-Up Display)**: панели с контентом, открывающиеся при клике на узлы 3D-сцены
- **Тур**: пошаговый walkthrough для новых пользователей (модуль `core/tour.js`)
- **Демо-режим**: быстрый доступ к ключевому контенту

Проект использует Three.js для 3D-рендеринга, модульную архитектуру ES6 и централизованную систему тем (Dark/Light).

**Архитектурные паттерны:**
- **Overlay/HUD паттерн (Фаза D):** HUD-режим и Guided-тур — две конфигурации одного паттерна управления вниманием пользователя (см. раздел "Общий паттерн Overlay / HUD")

### Зачем нужна эта архитектура

Архитектура определяет **жёсткие правила** (инварианты), которые гарантируют:

1. **Масштабируемость**: добавление новых тем, режимов и экранов без поломки существующего функционала
2. **Предсказуемость**: единые точки входа для критических операций (смена темы, монтирование сцены)
3. **Разделение ответственности**: чёткие границы между подсистемами (ядро, UI, темы, тур)
4. **Отсутствие конфликтов**: предотвращение ситуаций, когда несколько модулей пытаются управлять одним состоянием

Без явной архитектуры проект быстро превращается в "спагетти-код", где изменение одной части ломает другую, а добавление новых фич требует правок в десятках мест.

---

## Владение подсистемами

> ⚠️ **КРИТИЧНО**: Каждая подсистема имеет **единственного владельца**, который отвечает за её создание и жизненный цикл. Другие модули **не должны** изменять элементы, созданные другими владельцами.

### Владение хедером

> ⚠️ **ЖЁСТКИЙ КОНТРАКТ (Фаза B):** Каждый элемент хедера имеет единственного владельца. Другие модули **НЕ имеют права** манипулировать элементами, созданными другими владельцами.

**Владелец:** Разные модули владеют разными элементами хедера.

#### 1. `#theme-toggle-btn` → **HUD Manager** (`core/hud-manager.js`)

**Контракт владения:**
- **Создание:** Только в `createThemeToggleButton()` внутри `initHUD()`
- **Управление:** Только через `ensureThemeButton()` и `setupThemeToggleButton()`
- **Размещение:** В `#theme-toggle-container` или `#site-header` (по единому правилу)
- **Логика тем:** Вся логика (toggle, title, состояние) идёт через HUD → ThemeSwitcher → ThemeController

**Жёсткие запреты для других модулей:**
- ❌ **НЕЛЬЗЯ** искать `#theme-toggle-btn` через `getElementById()` или `querySelector()`
- ❌ **НЕЛЬЗЯ** менять `display`, `visibility`, `opacity`, `pointer-events` напрямую
- ❌ **НЕЛЬЗЯ** перемещать кнопку (`parentNode`, `appendChild`, `removeChild`)
- ❌ **НЕЛЬЗЯ** удалять кнопку

**Разрешённое взаимодействие:**
- ✅ Bootstrap и тур могут вызывать `window.hud.ensureThemeButton()` для стабилизации
- ✅ Это единственный способ "self-healing" после каких-то сценариев

**Результат:** Больше нет ситуации, когда три модуля одновременно "чинят" одну и ту же кнопку.

#### 2. `#tour-restart-btn` → **Bootstrap** (`index.html`) + **Tour** (видимость)

**Контракт владения:**
- **Создание:** Только в bootstrap-блоке `index.html` (один раз)
- **Управление видимостью:** Только функции тура (`showTour`/`hideTour`) через `style.display`
- **Запрещено:** Тур не имеет права влиять на тему или искать `#theme-toggle-btn`

**Жёсткие запреты для других модулей:**
- ❌ HUD и другие модули **не должны** изменять эту кнопку
- ❌ Тур **не должен** искать `#theme-toggle-btn` или манипулировать ей

#### 3. `#btn-demo`, `#btn-links`, `#btn-about` → **Статическая верстка** (`index.html`)

**Контракт владения:**
- **Создание:** В HTML-разметке (статическая верстка)
- **Обработчики:** Настраиваются в `core/hud-manager.js` при инициализации HUD
- **Запрещено:** Тур и другие модули **не должны** изменять эти кнопки без необходимости

**Важно:** `#btn-demo` — это **не** кнопка темы. Настоящая кнопка темы — `#theme-toggle-btn`, создаваемая HUD Manager.

### Владение HUD

**Владелец:** **HUD Manager** (`core/hud-manager.js`)

- Создаёт и управляет всеми HUD-панелями (`#hud-big-panel`, `#hud-small-panel`)
- Создаёт и управляет кнопкой темы (`#theme-toggle-btn`)
- Управляет оверлеем (`#hud-overlay`)
- Обрабатывает события от 3D-сцены (`nodeSelected`)
- **Запрещено:** Тур и другие модули **не должны** напрямую изменять HUD-панели

### Владение туром

**Владелец:** **Bootstrap** (`index.html`)

- Создаёт и управляет туром (`#tour-overlay`, `#tour-restart-btn`)
- Управляет шагами тура и сохранением прогресса
- Управляет классами `tour-active` для стилизации
- **Запрещено:** HUD и другие модули **не должны** напрямую изменять элементы тура

### Взаимодействие между подсистемами

**HUD ↔ Тур:**
- Тур может показывать/скрывать HUD через классы `tour-active`
- HUD не управляет туром напрямую
- Кнопка темы (создаваемая HUD) всегда видима, даже когда активен тур
- **Фаза B:** Тур может вызывать `window.hud.ensureThemeButton()` для стабилизации, но не манипулирует кнопкой напрямую
- **Фаза D:** HUD и тур — две конфигурации одного паттерна Overlay (см. раздел "Общий паттерн Overlay / HUD")

**HUD ↔ 3D-сцена:**
- 3D-сцена отправляет события `nodeSelected` → HUD отображает контент
- HUD не управляет 3D-сценой напрямую
- HUD читает цвета из `getThemeColors()` для стилизации

**Тур ↔ 3D-сцена:**
- Тур может ставить паузу рендеринга (только на мобильных)
- Тур не меняет тему и не монтирует сцену
- 3D-сцена не управляет туром напрямую

**Bootstrap ↔ HUD:**
- **Фаза B:** Bootstrap может вызывать `window.hud.ensureThemeButton()` для стабилизации кнопки темы
- Bootstrap **не имеет права** манипулировать `#theme-toggle-btn` напрямую
- Bootstrap создаёт `#tour-restart-btn`, но не управляет кнопкой темы

**Подробнее:** см. раздел 6 "Header Controls Ownership" ниже.

---

## Подсистемы

### 🎮 Ядро 3D-движка

**Файлы:**
- `core/engine.js` — главный движок (Three.js renderer, camera, scene, анимационный цикл)
- `core/state.js` — глобальное состояние и event bus
- `core/navigation.js` — управление камерой (скролл, орбитальный режим)
- `core/picking.js` — raycasting для взаимодействия с 3D-объектами
- `core/labels.js` — 2D-метки поверх 3D-сцены
- `core/sections.js` — валидация и маппинг секций контента
- `core/hud-manager.js` — менеджер HUD-панелей
- `core/theme-colors.js` — централизованная система цветов
- `core/tour.js` — модуль тура (конфигурация шагов, управление туром) (Фаза 3)

**Цель:**
Управление жизненным циклом 3D-сцены, рендеринг, взаимодействие с объектами, координация подсистем (navigation, picking, labels).

**Связи:**
- ✅ **Связана с:**
  - `scenes/calmScene-complete.js` — единственная активная 3D-сцена, монтируется через `engine.mount()`
  - `core/state.js` — читает/записывает `activeSceneId`, `activeSection`, `orbitModeEnabled`
  - `core/theme-colors.js` — читает цвета для фона сцены (но не меняет тему)
  - `core/hud-manager.js` — получает события `nodeSelected` от picking, управляет HUD через публичный API
  - `ui/theme-controller.js` — получает ссылку на engine для обновления фона сцены при смене темы

- ❌ **НЕ должна быть связана с:**  
  - `ui/theme-controller.js` — не вызывает `applyThemeById()` напрямую
  - `index.html` (логика тура) — не управляет туром напрямую
  - `themes/ui-theme-config.js` — не читает конфигурации UI-тем

**Ответственность:**
- Создание и управление Three.js renderer, camera, scene
- Анимационный цикл (с паузой на мобильных в туре)
- Монтирование/размонтирование плагинов сцен
- Координация navigation, picking, labels
- Эмиссия событий (`nodeSelected`, `themeMounted`)

---

### 🎨 Система тем

**Файлы:**
- `ui/theme-controller.js` — **единая точка применения темы** (`applyThemeById()`)
- `core/theme-colors.js` — централизованные цветовые токены (UI и 3D-сцена)
- `themes/ui-theme-config.js` — конфигурации UI-тем (dark/light)
- `styles/tokens.css` — CSS-переменные для тем
- `ui/themeSwitcher.js` — UI-обертка для инициализации (монтирует сцену один раз)

**Цель:**
Управление визуальным оформлением приложения (Dark/Light темы) без изменения логики и структуры 3D-сцены.

**Связи:**
- ✅ **Связана с:**
  - `core/state.js` — читает/записывает `activeThemeId`
  - `core/engine.js` — обновляет фон сцены при смене темы (через ссылку на engine)
  - `scenes/calmScene-complete.js` — сцена читает цвета из `getSceneColors()` при монтировании
  - `styles/tokens.css` — обновляет CSS-переменные через классы `.theme-dark` / `.theme-light`
  - `core/hud-manager.js` — создаёт кнопку переключения темы, вызывает `applyThemeById()`

- ❌ **НЕ должна быть связана с:**
  - `index.html` (логика тура) — тур не меняет тему
  - `core/engine.js` — не вызывает `engine.mount()` при смене темы
  - `themes/calm.js` — это конфигурация сцены, а не UI-тема

**Ответственность:**
- Единственная точка применения темы (`applyThemeById()`)
- Обновление CSS-классов (`.theme-dark` / `.theme-light`)
- Обновление CSS-переменных через `tokens.css`
- Обновление фона 3D-сцены при смене темы
- Сохранение выбора темы в `localStorage`
- Предоставление цветовых токенов для UI и 3D-сцены

**Источники истины для цветов:**
- **UI цвета:** `styles/tokens.css` — CSS-переменные для всех UI-компонентов (HUD, тур, кнопки, панели)
- **3D-сцена:** `core/theme-colors.js` — JavaScript-модуль с цветами для Three.js сцены (фон, узлы, орбиты, лучи, освещение)
- **Legacy/адаптеры:** `themes/theme-variables.css`, `themes/simple-themes.css`, `styles/variables.css` — старые файлы, кандидаты на удаление после миграции
- **Критические цвета:** Inline-стили в `index.html` (прелоадер, critical-* переменные) — необходимы для предотвращения FOUC

**Подробная карта цветов:** см. `docs/COLOR_MAP.md`

**Legacy цвета:**
- `styles/variables.css` — тонкий alias-слой для обратной совместимости (все переменные маппятся на токены)
- `themes/theme-variables.css` — **не используется**, готов к удалению
- `themes/simple-themes.css` — **не используется**, готов к удалению
- Новые компоненты/экраны (HUD/scene/tour) **должны использовать только токены** из `styles/tokens.css` и `core/theme-colors.js`, **НЕ** legacy-переменные

---

### 🖼️ 3D-сцена (плагин)

**Файлы:**
- `scenes/calmScene-complete.js` — единственная активная 3D-сцена, factory-функция для создания плагина сцены
- `themes/calm.js` — конфигурация сцены (палитра узлов, скорости, но не UI-тема)
- `themes/index.js` — реестр сцен (содержит только `calm`)

**Цель:**
Создание и управление 3D-объектами сцены (узлы, лучи, голограммы, эффекты). Сцена **не зависит** от UI-темы напрямую, но читает цвета из `getSceneColors()`.

**Связи:**
- ✅ **Связана с:**
  - `core/engine.js` — монтируется через `engine.mount()`, получает `scene`, `camera`, `renderer`
  - `core/theme-colors.js` — читает цвета через `getSceneColors()` при монтировании и обновлении
  - `core/sections.js` — использует `SECTION_NAMES` для валидации узлов
  - `data/nodes-data-complete.js` — читает контент для узлов

- ❌ **НЕ должна быть связана с:**
  - `ui/theme-controller.js` — не вызывает `applyThemeById()`
  - `themes/ui-theme-config.js` — не читает конфигурации UI-тем
  - `core/hud-manager.js` — не управляет HUD напрямую

**Ответственность:**
- Создание 3D-объектов (узлы, лучи, голограммы, эффекты)
- Обновление анимаций в `update(t, dt)`
- Предоставление якорей для меток (`getLabelAnchors()`)
- Предоставление активной камеры (`getActiveCamera()`)
- Очистка ресурсов при размонтировании (`dispose()`)

---

### 🎭 Общий паттерн Overlay / HUD (Фаза D)

> **Фаза D:** HUD-режим и Guided-тур — две конфигурации одного паттерна Overlay. Это не "два разных зверя", а единая модель управления вниманием пользователя.

**Общий паттерн Overlay:**

Overlay — это слой над базовым интерфейсом, который:
- **Управляет вниманием** — фокусирует пользователя на конкретном контенте
- **Показывает структурированный контент** — отображает информацию в организованном виде
- **Блокирует фоновые взаимодействия** — предотвращает случайные действия с базовым интерфейсом

**Две конфигурации одного паттерна:**

#### 1. HUD-режим (ручной, разделами)

**Характеристики:**
- **Режим:** Ручной — открывается пользователем при клике на узел 3D-сцены
- **Структура:** Разделами — навигация между секциями (Проблема, Исследования, Решения)
- **Точка входа:** Событие `nodeSelected` от 3D-сцены
- **Точка выхода:** Кнопка закрытия или клик вне панели

**Реализация:**
- Overlay: `#overlay` + `#hud-backdrop` (затемнение фона)
- Панели: `#hud-big-panel`, `#hud-small-panel`
- Классы: `hud-active` на `body`/`html` (блокировка скролла, стилизация)
- Функции: `showOverlay(node)`, `hideOverlay()`

#### 2. Guided-тур (сценарный, с шагами)

**Характеристики:**
- **Режим:** Сценарный — пошаговый walkthrough с автозапуском
- **Структура:** С шагами — последовательность шагов (1 → 2 → 3)
- **Точка входа:** Автозапуск при первой загрузке или кнопка "Перезапустить тур"
- **Точка выхода:** Кнопка "Пропустить" или завершение всех шагов

**Реализация:**
- Модуль: `core/tour.js` — вся логика тура (конфигурация шагов, функции управления)
- Overlay: `#tour-overlay` с `tour-backdrop` (затемнение фона)
- Карточка: `.tour-card` (контент тура)
- Классы: `tour-active` на `body`/`html` (блокировка скролла, стилизация)
- Функции: `showTour()`, `hideTour()`, `startTour(step)`, `setStep(step)`, `tour_reset()`

**Общие принципы (единая модель):**

1. **Управление скроллом/фоном:**
   - Блокировка скролла страницы через `position: fixed` + `overflow: hidden` на `body`/`html`
   - Разрешение скролла внутри контента (панели HUD или карточка тура)
   - Сохранение позиции скролла перед блокировкой (для HUD)

2. **Блокировка/разблокировка взаимодействия:**
   - Затемнение фона через backdrop (визуальная блокировка)
   - Блокировка взаимодействий с базовым интерфейсом через CSS (`pointer-events`, `touch-action`)
   - Разрешение взаимодействий внутри overlay (кнопки, скролл контента)

3. **Точки входа/выхода:**
   - **Вход:** Функция показа (`showOverlay()`, `showTour()`)
     - Устанавливает классы активности
     - Показывает overlay и контент
     - Блокирует скролл и взаимодействия
   - **Выход:** Функция скрытия (`hideOverlay()`, `hideTour()`)
     - Убирает классы активности
     - Скрывает overlay и контент
     - Восстанавливает скролл и взаимодействия

4. **Стилизация через классы:**
   - Классы активности на `body`/`html` (`hud-active`, `tour-active`)
   - CSS использует эти классы для:
     - Блокировки скролла
     - Стилизации элементов (например, квадратные углы в HUD)
     - Управления видимостью элементов

**Различия (конфигурации паттерна):**

| Аспект | HUD-режим | Guided-тур |
|--------|-----------|------------|
| **Триггер** | Клик на узел 3D-сцены | Автозапуск или кнопка |
| **Навигация** | Разделами (табы) | Шагами (последовательность) |
| **Состояние** | Не сохраняется | Сохраняется в `localStorage` |
| **Контент** | Динамический (из узла) | Статический (шаги тура) |
| **Закрытие** | Клик вне панели или кнопка | Кнопка "Пропустить" или завершение |

**Ориентир для будущих изменений:**

При изменении HUD или тура учитывать:
- ✅ Общие принципы (скролл, блокировка, точки входа/выхода) должны быть согласованными
- ✅ Классы активности (`hud-active`, `tour-active`) не должны конфликтовать
- ✅ Стилизация через CSS должна использовать единые токены из `tokens.css`
- ✅ Взаимодействие с базовым интерфейсом должно быть предсказуемым

**Результат:** Дальше любые изменения в HUD/туре делаются согласованно, а не как два разных мира.

---

### 🖥️ UI/HUD

**Файлы:**
- `core/hud-manager.js` — менеджер HUD-панелей
- `styles/main.css` — стили UI (использует токены из `tokens.css`)
- `index.html` (разметка) — структура HUD, панелей, кнопок

**Цель:**
Отображение контента в панелях при взаимодействии с 3D-сценой, навигация между секциями, управление кнопками интерфейса.

**Связи:**
- ✅ **Связана с:**
  - `core/engine.js` — получает события `nodeSelected` от picking, управляет HUD через публичный API
  - `data/nodes-data-complete.js` — читает контент для отображения
  - `core/sections.js` — использует для валидации секций
  - `ui/theme-controller.js` — вызывает `applyThemeById()` при клике на кнопку темы
  - `styles/tokens.css` — использует CSS-переменные для стилизации

- ❌ **НЕ должна быть связана с:**
  - `core/engine.js` — не создаёт и не монтирует engine напрямую
  - `scenes/calmScene-complete.js` — не управляет сценой напрямую
  - `index.html` (логика тура) — не управляет туром напрямую

**Ответственность:**
- Отображение панелей с контентом узлов
- Навигация между секциями (Проблема, Исследования, Решения)
- Обработка кнопок (Демо, Автор, Ссылки)
- Создание и управление кнопкой переключения темы
- Стилизация через CSS-переменные

**Реализация паттерна Overlay (см. раздел "Общий паттерн Overlay / HUD" выше):**
- Overlay: `#overlay` + `#hud-backdrop` (затемнение фона)
- Панели: `#hud-big-panel`, `#hud-small-panel`
- Классы: `hud-active` на `body`/`html` (блокировка скролла, стилизация)
- Функции: `showOverlay(node)`, `hideOverlay()`

---

### 🧭 Тур и Демо

**Файлы:**
- `index.html` (встроенная логика) — весь функционал тура (показ/скрытие, шаги, сохранение прогресса)
- `core/hud-manager.js` (кнопка "Демо") — открытие HUD с секцией "Основы UX для AI"

**Цель:**
Предоставление пошагового walkthrough для новых пользователей и быстрого доступа к ключевому контенту.

**Связи:**
- ✅ **Связана с:**
  - `core/engine.js` — может управлять паузой рендеринга (только на мобильных в туре)
  - `core/hud-manager.js` — использует HUD для отображения контента в демо-режиме
  - DOM (классы) — управляет классами `tour-active` для стилизации
  - `window.hud.ensureThemeButton()` — может вызывать для стабилизации хедера (Фаза C)

- ❌ **НЕ должна быть связана с:**
  - `ui/theme-controller.js` — **не вызывает `applyThemeById()`**
  - `core/engine.js` — **не вызывает `engine.mount()`**
  - `core/state.js` — **не меняет `activeThemeId`**
  - CSS-классы темы — **не меняет `.theme-dark` / `.theme-light`**
  - `window.location.reload()` — **не использует по умолчанию** (только как крайний fallback при явном флаге)

**Ответственность:**
- Отображение пошагового тура
- Управление шагами и сохранение прогресса
- Кнопка перезапуска тура (`tour_reset`)
- Кнопка "Демо" для быстрого доступа к контенту
- Управление классами `tour-active` для стилизации и паузы рендеринга

**Реализация паттерна Overlay (см. раздел "Общий паттерн Overlay / HUD" выше):**
- Overlay: `#tour-overlay` с `tour-backdrop` (затемнение фона)
- Карточка: `.tour-card` (контент тура)
- Классы: `tour-active` на `body`/`html` (блокировка скролла, стилизация)
- Функции: `showTour()`, `hideTour()`, `startTour(step)`, `setStep(step)`

**Контракт `tour_reset` (Фаза C):**
- **Обязан делать:**
  - Сбросить шаг тура (через `startTour(1)`)
  - Открыть overlay тура с первого шага (через `startTour` → `showTour`)
  - Обновить свои флаги (`tour_done`, `tour_step`) (через `startTour`)
- **НЕ имеет права делать:**
  - `window.location.reload()` по умолчанию (только как крайний fallback при явном флаге `?tour=reload`)
  - Сбрасывать локальное состояние темы
  - Трогать DOM за пределами overlay тура и `#tour-restart-btn`
- **Взаимодействие с HUD/темами:**
  - После перезапуска тура: HUD и тема не переинициализируются
  - Максимум — тур может попросить HUD убедиться, что хедер в живом состоянии (через `window.hud.ensureThemeButton()`)
  - НО НЕ через прямое лазание в DOM

**Результат:** Нажатие "перезапуск тура" влияет только на тур, а не на тему, не на HUD.

---

### ⚠️ Legacy / кандидаты на вынос

**Файлы:**
- `themes/theme-variables.css` ⚠️ — старые CSS-переменные (возможно, дублирует `tokens.css`)
- `themes/simple-themes.css` ⚠️ — простые темы (light, neon), возможно не используется
- `index.html` (монолитный) — большой объём inline JavaScript (логика тура, bootstrap)

**Статус:**
Требуют аудита на использование. Если не используются — кандидаты на удаление. Если используются — кандидаты на рефакторинг.

---

## Инварианты (жёсткие архитектурные правила)

> ⚠️ **КРИТИЧНО**: Эти правила **НЕ должны нарушаться** при любых изменениях кода. Нарушение инвариантов приводит к нестабильности системы и конфликтам между подсистемами.

### 1. Одна сцена, темы сцену не подменяют

**Правило:**
- В проекте существует **один активный плагин сцены** (сейчас `calmScene-complete`).
- Темы **не создают свои сцены** и **не вызывают `engine.mount()`**.
- Темы влияют только на **конфигурацию сцены** (цвета, интенсивность и т.п.), но не на выбор плагина.

**Что это означает:**
- `themes/calm.js` — это конфигурация сцены, а не UI-тема.
- `ui/theme-controller.js` при смене темы **не вызывает** `engine.mount()`.
- Сцена монтируется **один раз** при инициализации (в `ui/themeSwitcher.js`).
- При смене темы сцена **не перемонтируется**, а только обновляет цвета через `getSceneColors()`.

**Технический долг:**
- ✅ Текущая реализация соответствует этому правилу (сцена монтируется один раз в `initThemeSwitcher()`).

---

### 2. Единый источник правды по теме и единая точка применения

**Правило:**
- Текущая тема хранится в **одном месте**: `activeThemeId` в `core/state.js`.
- Смена темы всегда происходит через **одну функцию**: `applyThemeById(themeId)` в `ui/theme-controller.js`.
- Никакие другие модули не изменяют `.theme-*` классы и не лезут напрямую в `theme-colors` — всё идёт через эту функцию.

**Что это означает:**
- `applyThemeById()` выполняет **все** действия при смене темы:
  1. Обновляет `activeThemeId` в `core/state.js`
  2. Ставит/снимает классы `.theme-dark` / `.theme-light` на `<html>` и `<body>`
  3. Обновляет CSS-переменные через `tokens.css` (автоматически через классы)
  4. Обновляет фон 3D-сцены (если engine уже инициализирован)
  5. Сохраняет выбор в `localStorage`
  6. Может перезагрузить страницу (опционально)

- **Запрещено:**
  - Прямые манипуляции с `.theme-light` / `.theme-dark` (кроме `theme-controller.js`)
  - Прямые вызовы `localStorage.setItem('colorTheme', ...)` (кроме `theme-controller.js`)
  - Прямые вызовы `set('activeThemeId', ...)` в других модулях (кроме `theme-controller.js`)

**Технический долг:**
- ✅ Текущая реализация соответствует этому правилу (все изменения темы идут через `applyThemeById()`).

---

### 3. Тур и демо не меняют тему и не монтируют сцену

**Правило:**
- Модули тура и демо **не вызывают** `applyThemeById()`, `engine.mount()` или аналогичные функции.
- Тур/демо могут управлять HUD/камерой и статусами (`tour-active`, `demo-active`) только через публичный API ядра/UI.

**Что это означает:**
- Тур может:
  - Добавлять/убирать классы `tour-active` для стилизации и паузы рендеринга
  - Управлять отображением панелей тура
  - Сохранять прогресс в `localStorage` (но не тему)

- Тур **не может:**
  - Вызывать `applyThemeById()` или `toggleTheme()`
  - Вызывать `engine.mount()`
  - Менять `activeThemeId` в state
  - Менять CSS-классы `.theme-dark` / `.theme-light`

**Технический долг:**
- ✅ Текущая реализация соответствует этому правилу (тур не меняет тему, только управляет классами `tour-active`).

---

### 4. UI не знает деталей движка

**Правило:**
- UI-код (HUD, кнопки, панели) **не создаёт** и **не монтирует** engine напрямую.
- Взаимодействие UI с движком идёт через **фасады** (`theme-controller`, `navigation`, `hud-manager`, события).

**Что это означает:**
- `core/hud-manager.js` **не вызывает** `createEngine()` или `engine.mount()`.
- UI получает ссылку на engine через параметры функций (например, `initHUD({ engine })`).
- UI взаимодействует с движком через события (`nodeSelected`, `stateChanged`) и публичный API.

**Технический долг:**
- ✅ Текущая реализация соответствует этому правилу (UI не создаёт engine, получает ссылку через параметры).

---

### 5. Цвета только из токенов/тем, без "жёстких" hex в боевом коде

**Правило:**
- UI-цвета берутся только из `styles/tokens.css` и/или конфигов темы, а не задаются напрямую hex/rgb.
- Цвета для 3D-сцены берутся только из `core/theme-colors.js`.
- В боевом коде (кроме объявления палитры/тем) не допускается ручное использование `#xxxxxx`, `rgb(...)`, `new Color('red')` и т.п.

**Что это означает:**
- В CSS: использовать `var(--bg-base)`, `var(--text-primary)` и т.д., а не `#0B0F14` или `rgb(11, 15, 20)`.
- В JavaScript (UI): использовать `getComputedStyle(document.documentElement).getPropertyValue('--accent')`, а не `'#5B9CFF'`.
- В JavaScript (3D-сцена): использовать `getSceneColors()` и `toThreeColor()`, а не `new THREE.Color(0x0b0f14)`.

**Исключения:**
- Файлы определения палитры/тем (`core/theme-colors.js`, `styles/tokens.css`) могут содержать прямые hex/rgb значения.
- Критические inline-стили в `index.html` (для предотвращения FOUC) могут использовать CSS-переменные с fallback.

**Технический долг:**
- ⚠️ Требует аудита: возможны прямые hex-значения в `index.html` (inline-стили) и других файлах.

---

### 6. Header Controls Ownership (Владение элементами управления в хедере)

> **Примечание:** Этот раздел детализирует информацию из раздела "Владение подсистемами" выше. См. также раздел "Владение хедером" в начале документа.

> ⚠️ **ЖЁСТКИЙ КОНТРАКТ (Фаза B):** Каждый элемент управления в хедере имеет **единственного владельца**, который отвечает за его создание и жизненный цикл. Другие модули **НЕ имеют права** манипулировать элементами, созданными другими владельцами.

**Правило:**
- Каждый элемент управления в хедере имеет **единственного владельца**, который отвечает за его создание и жизненный цикл.
- Другие модули **не должны** изменять элементы управления, созданные другими владельцами (ни `display`, ни `parentNode`, ни другие свойства).

**Владение элементами:**

#### 1. `#theme-toggle-btn` — владелец: **HUD Manager** (`core/hud-manager.js`)

**Контракт:**
- Создаётся в `createThemeToggleButton()` внутри `initHUD()`
- Управляется через `ensureThemeButton()` и `setupThemeToggleButton()`
- Живёт в `#theme-toggle-container` или `#site-header` (по единому правилу)
- Вся логика тем (toggle, title, состояние) идёт через HUD → ThemeSwitcher → ThemeController

**Жёсткие запреты (Фаза B):**
- ❌ **Bootstrap НЕ может:**
  - Искать `#theme-toggle-btn` через `getElementById()` или `querySelector()`
  - Менять `display`, `visibility`, `opacity`, `pointer-events` напрямую
  - Перемещать или удалять кнопку
  - ✅ **Может только:** вызывать `window.hud.ensureThemeButton()` для стабилизации

- ❌ **Тур НЕ может:**
  - Искать `#theme-toggle-btn` в любом месте кода
  - Менять ей стили или перемещать
  - ✅ **Может только:** вызывать `window.hud.ensureThemeButton()` для стабилизации

**Результат:** Больше нет ситуации, когда три модуля одновременно "чинят" одну и ту же кнопку.

#### 2. `#tour-restart-btn` — владелец: **Bootstrap** (`index.html`) + **Tour** (видимость)

**Контракт:**
- Создаётся только в bootstrap-блоке `index.html` (один раз)
- Видимость управляется функциями тура (`showTour`/`hideTour`) через `style.display`
- Тур управляет только видимостью; никакого влияния на тему

**Жёсткие запреты:**
- ❌ HUD и другие модули **не должны** изменять эту кнопку
- ❌ Тур **не должен** искать `#theme-toggle-btn` или манипулировать ей

#### 3. `#btn-demo`, `#btn-links`, `#btn-about` — владелец: **Статическая верстка** (`index.html`)

**Контракт:**
- Создаются в HTML-разметке (статичная разметка)
- Обработчики событий настраиваются в `core/hud-manager.js` при инициализации HUD

**Жёсткие запреты:**
- ❌ Тур и другие модули **не должны** изменять эти кнопки без необходимости

**Что это означает:**
- Модули должны **только читать** элементы управления других модулей (например, для проверки видимости)
- Модули **не должны** изменять `display`, `visibility`, `parentNode` или другие свойства элементов, созданных другими модулями
- Если модулю нужно управлять видимостью элемента, он должен использовать публичный API владельца элемента

**Взаимодействие (Фаза B):**
- HUD создаёт кнопку темы и обеспечивает её стабильность через `ensureThemeButton()`
- Bootstrap и тур могут вызывать `window.hud.ensureThemeButton()` для стабилизации, но не манипулируют кнопкой напрямую
- Тур управляет видимостью кнопки перезапуска тура, но не трогает кнопку темы
- Bootstrap создаёт кнопки хедера, но не управляет кнопкой темы (это делает HUD)

**Технический долг:**
- ✅ Текущая реализация соответствует этому правилу (HUD создаёт `#theme-toggle-btn`, bootstrap создаёт `#tour-restart-btn`)
- ✅ Добавлено логирование жизненного цикла кнопки темы (Фаза A)
- ✅ Добавлена стабилизация кнопки темы через `ensureThemeButton()` (Фаза B)
- ✅ **Фаза B:** Удалены прямые манипуляции с `#theme-toggle-btn` из bootstrap
- ✅ **Фаза B:** Bootstrap и тур используют только HUD API для стабилизации кнопки

---

## Поток инициализации (application lifecycle)

Правильная последовательность инициализации приложения гарантирует, что все подсистемы инициализируются в правильном порядке и не конфликтуют друг с другом.

### 1. Загрузка страницы

**Файл:** `index.html` (bootstrap-скрипт)

**Действия:**
1. Импорт THREE.js через importmap
2. Импорт модулей (`core/engine.js`, `ui/theme-controller.js`, `ui/themeSwitcher.js`, `core/hud-manager.js`)

---

### 2. Инициализация темы (до создания engine)

**Файл:** `index.html` → `ui/theme-controller.js` → `initTheme()`

**Действия:**
1. Чтение сохранённой темы из `localStorage` (ключ `colorTheme`) или URL hash
2. Применение темы через `applyThemeById()` (без перезагрузки):
   - Обновление `activeThemeId` в `core/state.js`
   - Добавление классов `.theme-dark` / `.theme-light` на `<html>` и `<body>`
   - Обновление CSS-переменных через `tokens.css` (автоматически через классы)
   - Обновление meta-тегов `theme-color`

**Важно:** Тема инициализируется **до** создания engine, чтобы CSS-переменные были доступны при создании сцены.

---

### 3. Создание и инициализация engine

**Файл:** `index.html` → `core/engine.js` → `createEngine()`

**Действия:**
1. Создание Three.js renderer, camera, scene
2. Установка фона сцены из CSS-переменной `--scene-bg` (уже обновлённой темой)
3. Инициализация подсистем: picking, labels, sections
4. Регистрация engine в `theme-controller` через `setEngine(engine)`

**Важно:** Engine создаётся **после** инициализации темы, чтобы фон сцены соответствовал текущей теме.

---

### 4. Монтирование сцены (один раз)

**Файл:** `index.html` → `ui/themeSwitcher.js` → `initThemeSwitcher()` → `mountCalmScene()`

**Действия:**
1. Получение конфигурации сцены из `themes/calm.js`
2. Вызов `engine.mount(calmConfig)`
3. Сцена читает цвета из `getSceneColors()` при монтировании
4. Сцена создаёт 3D-объекты и возвращает плагин
5. Engine сохраняет ссылку на плагин и запускает анимационный цикл

**Важно:** Сцена монтируется **один раз** при инициализации. При смене темы сцена **не перемонтируется**, а только обновляет цвета.

---

### 5. Применение темы к сцене (если нужно)

**Файл:** `ui/theme-controller.js` → `applyThemeById()` (уже вызвана в шаге 2)

**Действия:**
1. Если engine уже инициализирован, обновляется фон сцены через `engine.scene.background`
2. Сцена может обновить цвета материалов через `getSceneColors()` (если плагин поддерживает)

**Важно:** Это происходит **после** монтирования сцены, если тема была изменена после инициализации.

---

### 6. Инициализация HUD и UI

**Файл:** `index.html` → `core/hud-manager.js` → `initHUD()`

**Действия:**
1. Создание HUD-панелей и кнопок
2. Подписка на события `nodeSelected` от picking
3. Создание кнопки переключения темы (вызывает `applyThemeById()` при клике)
4. Регистрация обработчиков для кнопок (Демо, Автор, Ссылки)

**Важно:** HUD инициализируется **после** монтирования сцены, чтобы иметь доступ к узлам и контенту.

---

### 7. Подключение тура и демо

**Файл:** `index.html` (встроенная логика тура)

**Действия:**
1. Инициализация тура (чтение прогресса из `localStorage`)
2. Подключение обработчиков для кнопок тура
3. Управление классами `tour-active` для стилизации и паузы рендеринга

**Важно:** Тур и демо подключаются **поверх** уже работающей сцены и UI. Они **не меняют тему** и **не монтируют сцену**.

---

### Диаграмма потока инициализации

```
1. Загрузка страницы (index.html)
   │
   ├─ 2. initTheme() → applyThemeById()
   │     ├─ Чтение из localStorage/hash
   │     ├─ Обновление state (activeThemeId)
   │     ├─ Добавление классов .theme-dark/.theme-light
   │     └─ Обновление CSS-переменных
   │
   ├─ 3. createEngine()
   │     ├─ Создание renderer, camera, scene
   │     ├─ Установка фона из --scene-bg
   │     └─ setEngine(engine) в theme-controller
   │
   ├─ 4. initThemeSwitcher() → mountCalmScene()
   │     ├─ engine.mount(calmConfig)
   │     ├─ Сцена читает цвета из getSceneColors()
   │     └─ Запуск анимационного цикла
   │
   ├─ 5. initHUD()
   │     ├─ Создание панелей и кнопок
   │     ├─ Подписка на события nodeSelected
   │     └─ Создание кнопки темы
   │
   └─ 6. Инициализация тура
       └─ Управление классами tour-active
```

---

## Принципы масштабирования

Архитектура должна позволять добавлять новые темы, режимы и экраны без изменения существующего кода и без риска поломки функционала.

### Добавление новой темы

**Шаги:**
1. Добавить конфигурацию в `themes/ui-theme-config.js`:
   ```javascript
   export const NEW_THEME_CONFIG = {
     id: 'new-theme',
     cssClasses: { html: ['theme-new'], body: ['theme-new'] },
     metaThemeColor: '#FFFFFF',
     // ...
   };
   ```

2. Добавить цветовые токены в `core/theme-colors.js`:
   ```javascript
   const newThemeColors = {
     bgBase: '#FFFFFF',
     textPrimary: '#000000',
     // ...
   };
   
   export function getThemeColors() {
     const isNew = document.documentElement.classList.contains('theme-new');
     if (isNew) return newThemeColors;
     // ... existing logic
   }
   ```

3. Добавить CSS-переменные в `styles/tokens.css`:
   ```css
   :root.theme-new {
     --bg-base: #FFFFFF;
     --text-primary: #000000;
     /* ... */
   }
   ```

4. Обновить `ui/theme-controller.js` для поддержки новой темы:
   ```javascript
   export function applyThemeById(themeId, options = {}) {
     if (themeId !== 'dark' && themeId !== 'light' && themeId !== 'new-theme') {
       // validation
     }
     // ... existing logic
   }
   ```

**Что НЕ нужно делать:**
- ❌ Создавать новую сцену (`scenes/newScene.js`)
- ❌ Вызывать `engine.mount()` при смене темы
- ❌ Менять логику монтирования сцены

**Инварианты:**
- ✅ Одна сцена остаётся активной
- ✅ Смена темы идёт через `applyThemeById()`
- ✅ Цвета берутся из `theme-colors.js`

---

### Добавление нового режима (например, новый тип тура)

**Шаги:**
1. Создать новый модуль (например, `ui/advanced-tour.js`):
   ```javascript
   export function initAdvancedTour(engine, hud) {
     // Управление новым режимом
     // НЕ вызывает applyThemeById() или engine.mount()
   }
   ```

2. Подключить в `index.html` (bootstrap):
   ```javascript
   const { initAdvancedTour } = await import('./ui/advanced-tour.js');
   initAdvancedTour(engine, hud);
   ```

3. Управление через классы и события:
   ```javascript
   document.body.classList.add('advanced-tour-active');
   // Engine автоматически паузит рендеринг (если нужно)
   ```

**Что НЕ нужно делать:**
- ❌ Вызывать `applyThemeById()` или `toggleTheme()`
- ❌ Вызывать `engine.mount()` или `engine.unmount()`
- ❌ Менять `activeThemeId` в state
- ❌ Менять CSS-классы `.theme-dark` / `.theme-light`

**Инварианты:**
- ✅ Режим не меняет тему
- ✅ Режим не монтирует сцену
- ✅ Режим управляет только своими классами и событиями

---

### Добавление новых UI-экранов/панелей

**Шаги:**
1. Добавить разметку в `index.html`:
   ```html
   <div id="new-panel" class="hud-panel hidden">
     <!-- content -->
   </div>
   ```

2. Добавить стили в `styles/main.css` (используя токены из `tokens.css`):
   ```css
   #new-panel {
     background: var(--panel);
     color: var(--text-primary);
     border: 1px solid var(--border);
   }
   ```

3. Управление через `core/hud-manager.js` или отдельный модуль:
   ```javascript
   export function showNewPanel() {
     const panel = document.getElementById('new-panel');
     panel.classList.remove('hidden');
   }
   ```

**Что НЕ нужно делать:**
- ❌ Изменять `core/engine.js` для поддержки нового экрана
- ❌ Создавать новую сцену для нового экрана
- ❌ Использовать прямые hex-значения в стилях

**Инварианты:**
- ✅ UI не знает деталей движка
- ✅ Цвета берутся из токенов
- ✅ Новый экран не влияет на существующий функционал

---

### Добавление новой 3D-сцены (если потребуется в будущем)

**Текущее состояние:** Проект использует только одну 3D-сцену (`calmScene-complete`). Matrix-сцена удалена (Фаза 2).

**Шаги:**
1. Создать новый плагин сцены (например, `scenes/newScene.js`):
   ```javascript
   export function newSceneFactory(config) {
     return {
       mount({ scene, camera, renderer }) {
         // Создание 3D-объектов
         // Чтение цветов из getSceneColors()
       },
       update(t, dt) { /* ... */ },
       dispose() { /* ... */ },
       getLabelAnchors() { /* ... */ },
       getActiveCamera() { return camera; }
     };
   }
   ```

2. Добавить конфигурацию в `themes/new-scene.js`:
   ```javascript
   export const newScene = {
     id: 'new-scene',
     name: 'New Scene',
     sceneFactory: newSceneFactory,
     config: { /* ... */ }
   };
   ```

3. Зарегистрировать в `themes/index.js`:
   ```javascript
   import { newScene } from './new-scene.js';
   export const SCENES = {
     calm: calm,
     newScene: newScene
   };
   ```

4. Монтировать через `engine.mount(newScene)` (один раз при инициализации)

**Важно:**
- Новая сцена **не должна** создавать свою UI-тему
- Новая сцена должна читать цвета из `getSceneColors()`
- Монтирование сцены происходит **один раз** при инициализации, а не при смене темы

---

## Технический долг и отклонения от целевой архитектуры

### Текущие отклонения

1. **Монолитный `index.html`**
   - **Статус:** ✅ **Исправлено (Фаза 3)** — логика тура вынесена в модуль `core/tour.js`
   - **Приоритет:** Решено

2. **Дублирование логики создания кнопок**
   - **Проблема:** Кнопка темы создаётся в `hud-manager.js`, кнопка tour-restart — в `index.html`
   - **Рекомендация:** Унифицировать создание кнопок header в одном месте
   - **Приоритет:** Низкий

3. **Несколько MutationObserver**
   - **Проблема:** В `index.html` есть MutationObserver для tour-restart, в `hud-manager.js` — для защиты кнопки темы
   - **Рекомендация:** Проверить взаимодействие observers, возможно объединить
   - **Приоритет:** Низкий

4. **Возможные прямые hex-значения**
   - **Проблема:** Возможны прямые hex-значения в `index.html` (inline-стили) и других файлах
   - **Рекомендация:** Аудит всех файлов на наличие прямых hex/rgb значений (кроме `theme-colors.js` и `tokens.css`)
   - **Приоритет:** Высокий

5. **Подозрительные файлы**
   - **Проблема:** `themes/theme-variables.css` и `themes/simple-themes.css` могут дублировать функциональность `tokens.css`
   - **Рекомендация:** Аудит использования этих файлов, удаление если не используются
   - **Приоритет:** Средний

---

## Заключение

Эта архитектура обеспечивает:

1. **Масштабируемость**: добавление новых тем, режимов и экранов без поломки существующего функционала
2. **Предсказуемость**: единые точки входа для критических операций
3. **Разделение ответственности**: чёткие границы между подсистемами
4. **Отсутствие конфликтов**: предотвращение ситуаций, когда несколько модулей управляют одним состоянием

**Следующие шаги:**
- Фаза 2C: Рефакторинг кода для соответствия целевой архитектуре
- Аудит технического долга и устранение отклонений
- Добавление новых тем и режимов по описанным принципам

---

**Документ создан:** Фаза 2B (целевая архитектура)  
**Базовый baseline:** dev-ветка (стабильное состояние)  
**Следующий шаг:** Фаза 2C — рефакторинг кода

