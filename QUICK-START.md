# Быстрый старт после рефакторинга

## 🎉 Что сделано

Проведен комплексный рефакторинг UX4AI.PRO **без изменения визуала и функциональности**.

### ✅ Основные улучшения:

1. **CSS-токены Synaptik DS** — 80+ переменных для цветов, отступов, типографики
2. **Service Worker** — умное кеширование с версионированием
3. **Lazy-load** — все изображения с отложенной загрузкой
4. **No-JS fallback** — корректная работа без JavaScript
5. **Мобильная оптимизация** — full-width кнопки, нет горизонтального скролла
6. **SEO улучшения** — расширенные мета-теги и JSON-LD

## 🚀 Как запустить локально

### 1. Проверить изменения

```bash
# Убедитесь, что вы на ветке dev-refactor
git branch
# должно показать: * dev-refactor

# Посмотреть изменения
git diff main..dev-refactor --stat
```

### 2. Запустить локальный сервер

**Python:**
```bash
python -m http.server 8000
```

**Node.js (http-server):**
```bash
npx http-server -p 8000
```

**VS Code:**
Установите расширение "Live Server" и нажмите "Go Live"

### 3. Открыть в браузере

```
http://localhost:8000
```

## 🧪 Тестирование

### Проверить Service Worker

1. Откройте DevTools → Application → Service Workers
2. Должен показывать: "sw.js" со статусом "activated and running"
3. В Console: `[SW] Registered: http://localhost:8000/`

### Проверить lazy-load

1. DevTools → Network → Img
2. Прокрутите страницу
3. Изображения должны загружаться по мере появления в viewport

### Проверить no-JS fallback

1. DevTools → Settings → Debugger → Disable JavaScript
2. Перезагрузите страницу
3. Должно показаться сообщение с кнопкой "Скачать резюме"

### Lighthouse-аудит

```bash
# Установить (если нет)
npm install -g lighthouse

# Запустить
lighthouse http://localhost:8000 --view

# Проверить метрики:
# Performance ≥ 85
# Accessibility ≥ 90
# CLS < 0.1
# LCP < 2.5s
```

## 📱 Тестирование на мобильных

### Chrome DevTools

1. DevTools → Toggle device toolbar (Ctrl+Shift+M)
2. Выбрать устройство (iPhone 12, Pixel 5)
3. Проверить:
   - ✅ Нет горизонтального скролла
   - ✅ Кнопки во всю ширину, min-height 44px
   - ✅ Интерактивы (TTFT) на 100% ширины
   - ✅ Тур открывается корректно

### Реальное устройство

1. Узнать IP компьютера: `ipconfig` (Windows) или `ifconfig` (Mac/Linux)
2. На телефоне открыть: `http://[IP]:8000`
3. Проверить тач-взаимодействия

## 🎨 Работа с токенами

### Изменить цвет акцента

**Файл:** `styles/tokens.css`

```css
:root {
  --Color/Accent/Primary: #5B9CFF; /* Синий → Ваш цвет */
}
```

### Добавить новую тему

```css
:root.theme-custom {
  --Color/Dark/Background/Primary: #your-color;
  --Color/Accent/Primary: #your-accent;
  /* ... остальные переменные */
}
```

### Использовать токены в коде

```css
/* ❌ Старый способ */
background: rgba(18, 23, 34, 0.90);

/* ✅ Новый способ */
background: var(--Color/Dark/Surface/Base);
```

## 🔄 Обновление версии

### При деплое новой версии:

**Файл:** `sw.js`

```javascript
// Изменить версию
const BUILD_VERSION = '1.0.1'; // было 1.0.0
```

Пользователи автоматически увидят баннер с кнопкой "Обновить".

### Принудительное обновление

В консоли браузера:
```javascript
window.forceUpdate()
```

## 📊 Мониторинг производительности

### Console logs для аналитики

```javascript
// Просмотр страницы
console.log('analytics: page_view');

// Взаимодействие с метрикой
console.log('analytics: metric_interact', metricName);

// Показ уведомления об обновлении
console.log('analytics: update_notification_shown');
```

### Yandex.Metrika

Уже настроен в `index.html` (ID: 105120842). Проверить работу:
1. Перейти в Yandex.Metrika
2. Отчеты → Стандартные отчёты → Посещаемость

## 🔧 Возможные проблемы

### Service Worker не регистрируется

**Решение:**
- Проверьте, что сайт открыт через localhost или https
- DevTools → Application → Clear site data

### Lazy-load не работает

**Решение:**
- Проверьте браузер (должен быть современный)
- Fallback: изображения загрузятся при прокрутке

### Баннер обновления не показывается

**Решение:**
- Измените BUILD_VERSION в sw.js
- Hard reload (Ctrl+Shift+R)

## 📝 Следующие шаги

### Перед мержем в main:

1. ✅ Локальное тестирование
2. ✅ Lighthouse-аудит (≥85 Performance, ≥90 Accessibility)
3. ✅ Тестирование на мобильных (iOS + Android)
4. ✅ Проверка всех интерактивов (тур, HUD, TTFT)
5. ✅ Проверка Service Worker (кеш, обновления)

### После успешного тестирования:

```bash
# Переключиться на main
git checkout main

# Мердж dev-refactor
git merge dev-refactor

# Пуш (если нужно)
# git push origin main
```

## 📖 Документация

- **Полный отчёт:** `REFACTOR-REPORT.md`
- **Архитектура:** `ARCHITECTURE.md`
- **Статус миграции:** `MIGRATION-STATUS.md`

## ❓ Вопросы?

Все изменения **обратно совместимы** и **не ломают функциональность**.

Если что-то работает не так — проверьте DevTools Console на ошибки.

---

**Версия:** 1.0.0  
**Дата:** 12 ноября 2025  
**Ветка:** dev-refactor

