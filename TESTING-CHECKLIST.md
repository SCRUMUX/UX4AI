# Чеклист тестирования рефакторинга UX4AI.PRO

## 🚀 Быстрая проверка (5 минут)

### 1. Запустить локальный сервер

```bash
# Python
python -m http.server 8000

# Или Node.js
npx http-server -p 8000
```

Откройте: `http://localhost:8000`

### 2. Базовая проверка функциональности

- [ ] **Страница загружается** без ошибок в консоли
- [ ] **3D-сцена рендерится** (видно 3D-пространство)
- [ ] **Хедер отображается** (Владимир Костял, кнопки)
- [ ] **Тур запускается** (кнопка "Демо" или автоматически)
- [ ] **HUD открывается** (клик по метке в 3D)
- [ ] **Темы переключаются** (кнопка темы в хедере)

---

## 🔍 Детальная проверка (15 минут)

### 3. Service Worker

**DevTools → Application → Service Workers**

- [ ] Видно: `sw.js` со статусом **"activated and running"**
- [ ] В Console: `[SW] Registered: http://localhost:8000/`
- [ ] В Console: `[SW] Service Worker loaded, version: 1.0.0`

**Проверка кеширования:**
- [ ] DevTools → Application → Cache Storage
- [ ] Видно кеш: `ux4ai-v1.0.0`
- [ ] В кеше есть файлы: `index.html`, `tokens.css`, `main.css`

**Проверка обновлений:**
1. Измените `BUILD_VERSION` в `sw.js` на `1.0.1`
2. Перезагрузите страницу
3. Должен появиться **баннер "Доступна новая версия"**
4. Нажмите "Обновить" → страница перезагрузится

### 4. Lazy-load изображений

**DevTools → Network → Img**

- [ ] При первой загрузке загружаются только видимые изображения
- [ ] Прокрутите страницу вниз
- [ ] Новые изображения загружаются по мере появления в viewport
- [ ] В атрибутах `<img>` есть `loading="lazy"`

**Проверка:**
- Откройте Network tab
- Установите фильтр "Img"
- Прокрутите страницу
- Изображения должны загружаться постепенно

### 5. No-JS Fallback

**DevTools → Settings (⚙️) → Debugger → Disable JavaScript**

- [ ] Перезагрузите страницу (Ctrl+R)
- [ ] Должно показаться **сообщение**: "Интерактивные элементы требуют JavaScript"
- [ ] Видна кнопка **"Скачать резюме (PDF)"**
- [ ] Есть ссылка на Telegram

**Вернуть JS:**
- DevTools → Settings → Debugger → **снимите** "Disable JavaScript"
- Перезагрузите страницу

### 6. CSS-токены

**DevTools → Elements → выберите любой элемент**

- [ ] В Styles видно использование `var(--Color/...)`
- [ ] Нет hardcoded цветов типа `#5B9CFF` или `rgba(91, 156, 255, ...)`
- [ ] Используются токены: `var(--Color/Accent/Primary)`, `var(--Spacing/...)`

**Проверка переменных:**
- DevTools → Elements → `<html>` элемент
- В Styles найдите `:root`
- Должны быть видны все токены из `tokens.css`

### 7. Мобильная версия

**DevTools → Toggle device toolbar (Ctrl+Shift+M)**

**Выберите устройство:** iPhone 12 или Pixel 5

**Проверки:**
- [ ] **Нет горизонтального скролла** (overflow-x: hidden)
- [ ] **Кнопки во всю ширину** (width: 100%)
- [ ] **Кнопки имеют min-height: 44px** (Apple HIG)
- [ ] **Тур открывается корректно** (карточка на всю ширину)
- [ ] **HUD панели адаптируются** (вертикальный стек)
- [ ] **Интерактивы (TTFT) на 100% ширины**
- [ ] **Текст не обрезается** (word-wrap: break-word)

**Проверка тач-взаимодействий:**
- [ ] Кнопки реагируют мгновенно (нет задержки)
- [ ] Нет выделения текста при тапе
- [ ] Плавная прокрутка

### 8. Визуальная идентичность

**Сравнение с оригиналом:**

- [ ] **Цвета идентичны** (акцент #5B9CFF, фон #0B0F14)
- [ ] **Шрифты те же** (ui-sans-serif, system-ui)
- [ ] **Размеры элементов** не изменились
- [ ] **Анимации работают** (переходы, hover-эффекты)
- [ ] **3D-сцена выглядит так же**

---

## 📊 Lighthouse-аудит (10 минут)

### Установка Lighthouse

```bash
npm install -g lighthouse
```

### Запуск аудита

```bash
lighthouse http://localhost:8000 --view
```

### Целевые метрики

| Метрика | Цель | Проверка |
|---------|------|----------|
| **Performance** | ≥ 85 | [ ] |
| **Accessibility** | ≥ 90 | [ ] |
| **Best Practices** | ≥ 90 | [ ] |
| **SEO** | ≥ 90 | [ ] |
| **CLS** | < 0.1 | [ ] |
| **LCP** | < 2.5s | [ ] |
| **FID** | < 100ms | [ ] |

### Что проверить в отчёте

- [ ] **LCP (Largest Contentful Paint)** — должен быть < 2.5s
- [ ] **CLS (Cumulative Layout Shift)** — должен быть < 0.1
- [ ] **FCP (First Contentful Paint)** — должен быть < 1.8s
- [ ] **TTI (Time to Interactive)** — должен быть < 3.8s
- [ ] **Total Blocking Time** — должен быть < 200ms

**Обратите внимание на:**
- ✅ "Uses efficient cache policies" (Service Worker)
- ✅ "Image elements have explicit width and height" (lazy-load)
- ✅ "Document has a valid `lang` attribute"
- ✅ "Links are crawlable"

---

## 🐛 Проверка ошибок

### Console (DevTools → Console)

- [ ] **Нет ошибок** (красные сообщения)
- [ ] Видны логи: `[SW] Registered`, `[Bootstrap] ✅ Ready!`
- [ ] Нет предупреждений о загрузке ресурсов

### Network (DevTools → Network)

- [ ] Все ресурсы загружаются (статус 200)
- [ ] Нет 404 ошибок
- [ ] `tokens.css` и `main.css` загружаются
- [ ] `sw.js` загружается

### Application (DevTools → Application)

- [ ] **Service Workers** — активен
- [ ] **Cache Storage** — есть кеш `ux4ai-v1.0.0`
- [ ] **Storage** — нет ошибок

---

## 🎯 Функциональные тесты

### Тур (Guided Tour)

- [ ] **Автозапуск** работает (если не был завершён ранее)
- [ ] **Кнопки "Назад/Далее"** работают
- [ ] **Ссылки в туре** открываются в новой вкладке
- [ ] **Интерактивы (TTFT)** загружаются и работают
- [ ] **Закрытие тура** (ESC или кнопка) работает

### HUD (Heads-Up Display)

- [ ] **Клик по метке** открывает HUD
- [ ] **Панели отображаются** корректно
- [ ] **Навигация между секциями** работает
- [ ] **Закрытие HUD** (ESC или клик вне панели) работает
- [ ] **Ссылки в HUD** открываются корректно

### 3D-сцена

- [ ] **Камера движется** при прокрутке
- [ ] **Метки отображаются** в правильных позициях
- [ ] **Hover-эффекты** на метках работают
- [ ] **Орбитальный режим** (если есть) работает

### Темы

- [ ] **Переключение темы** работает
- [ ] **Calm тема** применяется корректно
- [ ] **Matrix тема** применяется корректно
- [ ] **Токены обновляются** при смене темы

---

## 📱 Тестирование на реальном устройстве

### Подготовка

1. Узнайте IP компьютера:
   ```bash
   # Windows
   ipconfig
   # Mac/Linux
   ifconfig
   ```

2. На телефоне откройте: `http://[IP]:8000`

### Проверки на мобильном

- [ ] **Страница загружается** без ошибок
- [ ] **Нет горизонтального скролла**
- [ ] **Кнопки легко нажимаются** (min-height 44px)
- [ ] **Тур открывается** корректно
- [ ] **3D-сцена рендерится** (может быть медленнее)
- [ ] **Тач-жесты** работают плавно
- [ ] **Service Worker** регистрируется (проверить в мобильном Chrome DevTools)

---

## ✅ Финальный чеклист

### Перед мерджем в main:

- [ ] Все функциональные тесты пройдены
- [ ] Lighthouse Performance ≥ 85
- [ ] Lighthouse Accessibility ≥ 90
- [ ] Нет ошибок в Console
- [ ] Мобильная версия работает корректно
- [ ] Service Worker работает
- [ ] Lazy-load работает
- [ ] No-JS fallback работает
- [ ] Визуально идентичен оригиналу

---

## 🚨 Известные проблемы и решения

### Service Worker не регистрируется

**Решение:**
- Убедитесь, что сайт открыт через `localhost` или `https`
- DevTools → Application → Clear site data
- Перезагрузите страницу

### Lazy-load не работает

**Решение:**
- Проверьте браузер (должен быть современный: Chrome 76+, Firefox 75+)
- Fallback: изображения загрузятся при прокрутке

### Баннер обновления не показывается

**Решение:**
- Измените `BUILD_VERSION` в `sw.js`
- Hard reload (Ctrl+Shift+R)

### Мобильная версия не адаптируется

**Решение:**
- Проверьте, что `main.css` загружается
- DevTools → Elements → проверьте применённые стили
- Убедитесь, что media queries работают

---

## 📝 Заметки для тестирования

**Дата тестирования:** _______________  
**Тестировщик:** _______________  
**Браузер:** _______________  
**Версия:** _______________

**Найденные проблемы:**
1. 
2. 
3. 

**Рекомендации:**
1. 
2. 
3. 

---

*Чеклист создан для рефакторинга версии 1.0.0*

