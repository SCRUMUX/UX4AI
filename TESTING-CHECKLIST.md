# Чеклист тестирования рефакторинга UX4AI.PRO

**Версия:** Фаза B — Унификация путей и гибкость запуска  
**Последнее обновление:** Фаза B — Добавлены тесты для сценариев с изменённым базовым путём

---

## 🎯 Ключевые сценарии использования (Фаза 7)

### Сценарий 1: Загрузка сайта

#### 1.1. Новый пользователь (первый визит)

**Шаги:**
1. Откройте сайт в режиме инкогнито (или очистите localStorage)
2. Откройте DevTools → Console
3. Дождитесь полной загрузки страницы

**Ожидаемый результат:**
- [ ] **Прелоадер отображается** (`ux4ai-preloader-loop-outline-noise.html` загружается в iframe)
- [ ] **Прелоадер скрывается** после инициализации 3D-сцены
- [ ] **3D-сцена рендерится** (видно 3D-пространство с узлами)
- [ ] **Хедер отображается** (Владимир Костял, кнопки)
- [ ] **Кнопка темы (`#theme-toggle-btn`) видна** и находится в `#theme-toggle-container`
- [ ] **Авто-тур запускается** (если не был завершён ранее)
- [ ] **Service Worker регистрируется** (в консоли: `[SW] Registered`)
- [ ] **Нет ошибок в консоли** (красные сообщения)

**Проверка в DevTools:**
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка существует
- [ ] Elements → найдите `#preloader-overlay` → убедитесь, что прелоадер скрыт (класс `hidden`)
- [ ] Application → Service Workers → убедитесь, что SW активен
- [ ] Console → проверьте логи: `[Bootstrap] ✅ Ready!`, `[HUD][ThemeButton] created`

#### 1.2. Возвращающийся пользователь (returning)

**Шаги:**
1. Откройте сайт повторно (localStorage содержит сохранённую тему и состояние тура)
2. Откройте DevTools → Application → Local Storage
3. Проверьте сохранённые значения: `colorTheme`, `tour_done`, `tour_step`

**Ожидаемый результат:**
- [ ] **Тема восстанавливается** из localStorage (Dark/Light)
- [ ] **Тур не запускается автоматически** (если `tour_done === '1'`)
- [ ] **Кнопка "Перезапустить тур" видна** (если тур был завершён)
- [ ] **3D-сцена загружается** с правильными цветами для сохранённой темы
- [ ] **Кнопка темы (`#theme-toggle-btn`) видна** и работает
- [ ] **Service Worker использует кеш** (быстрая загрузка)

**Проверка в DevTools:**
- [ ] Application → Local Storage → проверьте `colorTheme` (должно быть `dark` или `light`)
- [ ] Elements → `<html>` → проверьте классы `theme-dark` или `theme-light`
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка существует и видна

---

### Сценарий 2: Авто-тур и его перезапуск

#### 2.1. Автозапуск тура (новый пользователь)

**Шаги:**
1. Очистите localStorage (или откройте в режиме инкогнито)
2. Перезагрузите страницу
3. Дождитесь автозапуска тура

**Ожидаемый результат:**
- [ ] **Тур открывается автоматически** (overlay `#tour-overlay` виден)
- [ ] **Первый шаг тура отображается** (шаг 1)
- [ ] **Кнопка темы (`#theme-toggle-btn`) остаётся видимой** (не скрывается туром)
- [ ] **Кнопка "Перезапустить тур" скрыта** (пока тур активен)
- [ ] **Навигация "Назад/Далее" работает**
- [ ] **Закрытие тура (ESC) работает**

**Проверка в DevTools:**
- [ ] Elements → найдите `#tour-overlay` → убедитесь, что overlay виден (нет класса `hidden`)
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка видна (`display: inline-flex`)
- [ ] Elements → найдите `#tour-restart-btn` → убедитесь, что кнопка скрыта (`display: none`)
- [ ] Console → проверьте логи: `[Tour] Starting tour from step 1`

#### 2.2. Перезапуск тура (кнопка "Перезапустить тур")

**Шаги:**
1. Завершите тур (пройдите все шаги или закройте ESC)
2. Нажмите кнопку "Перезапустить тур" (`#tour-restart-btn`)
3. Проверьте состояние после перезапуска

**Ожидаемый результат:**
- [ ] **Тур открывается с первого шага** (шаг 1)
- [ ] **localStorage обновляется** (`tour_done` удалён, `tour_step` = `1`)
- [ ] **Кнопка темы (`#theme-toggle-btn`) остаётся видимой** (не исчезает)
- [ ] **Кнопка темы работает** (можно переключить тему во время тура)
- [ ] **Нет перезагрузки страницы** (`window.location.reload()` не вызывается)
- [ ] **HUD и тема не переинициализируются** (состояние сохраняется)
- [ ] **startTour() возвращает true** (в консоли нет ошибок о неудачном запуске)

**Проверка в DevTools:**
- [ ] Elements → найдите `#tour-overlay` → убедитесь, что overlay виден
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка видна и работает
- [ ] Application → Local Storage → проверьте `tour_done` (должно быть удалено) и `tour_step` (должно быть `1`)
- [ ] Console → проверьте логи: `[Tour] Starting tour from step 1`, `[HUD][ThemeButton] ensureThemeButton result`
- [ ] Console → убедитесь, что нет вызова `window.location.reload()`
- [ ] Console → проверьте, что нет ошибок `[Tour] CRITICAL: Cannot start tour`

#### 2.3. Перезапуск тура на разных шагах

**Шаги:**
1. Запустите тур (автозапуск или кнопка "Демо")
2. Перейдите на шаг 2 (кнопка "Кейсы")
3. Нажмите кнопку "Перезапустить тур"
4. Повторите для шага 3

**Ожидаемый результат:**
- [ ] **Тур всегда перезапускается с шага 1** независимо от текущего шага
- [ ] **startTour(1) всегда возвращает true** при наличии шага 1
- [ ] **Нет ошибок в консоли** о недостающих шагах или DOM элементах

**Проверка в DevTools:**
- [ ] Console → проверьте логи: `[Tour] Starting tour from step 1`
- [ ] Console → убедитесь, что нет ошибок `[Tour] CRITICAL: step 1 config is missing`
- [ ] Console → убедитесь, что нет ошибок `[Tour] CRITICAL: tour-overlay element not found`

#### 2.4. Смена темы до/после тура

**Шаги:**
1. Переключите тему (Dark ↔ Light) до запуска тура
2. Запустите тур (автозапуск или кнопка "Демо")
3. Переключите тему во время тура
4. Закройте тур
5. Переключите тему после тура

**Ожидаемый результат:**
- [ ] **Тема переключается до тура** без проблем
- [ ] **Тема переключается во время тура** (тур остаётся открытым)
- [ ] **Тема переключается после тура** без проблем
- [ ] **Тур не влияет на переключение темы** (кнопка темы всегда работает)
- [ ] **Кнопка темы (`#theme-toggle-btn`) всегда видна** во всех состояниях

**Проверка в DevTools:**
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка всегда видна
- [ ] Console → проверьте логи: `[ThemeController] Applying theme: ...`
- [ ] Console → убедитесь, что нет ошибок от тура при переключении темы

#### 2.5. Открытие HUD во время тура

**Шаги:**
1. Запустите тур (автозапуск или кнопка "Демо")
2. Откройте HUD (клик по метке в 3D-сцене)
3. Закройте HUD (ESC)
4. Закройте тур (ESC)

**Ожидаемый результат:**
- [ ] **HUD открывается во время тура** (оба overlay видны)
- [ ] **Тур остаётся открытым** при открытии HUD
- [ ] **Кнопка темы (`#theme-toggle-btn`) остаётся видимой** и работает
- [ ] **Нет конфликтов** между туром и HUD

**Проверка в DevTools:**
- [ ] Elements → найдите `#tour-overlay` → убедитесь, что overlay виден
- [ ] Elements → найдите `#hud-overlay` → убедитесь, что overlay виден
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка видна и работает
- [ ] Console → убедитесь, что нет ошибок при взаимодействии тура и HUD

#### 2.6. Стабильность кнопки темы при перезапуске тура (5 циклов)

**Шаги:**
Выполните **5 циклов** следующей последовательности:
1. Перезапустите тур (кнопка "Перезапустить тур")
2. Переключите тему (кнопка темы)
3. Закройте тур (ESC)

**Ожидаемый результат после каждого цикла:**
- [ ] **Цикл 1:** Кнопка темы видна и работает
- [ ] **Цикл 2:** Кнопка темы видна и работает
- [ ] **Цикл 3:** Кнопка темы видна и работает
- [ ] **Цикл 4:** Кнопка темы видна и работает
- [ ] **Цикл 5:** Кнопка темы видна и работает

**Проверка в DevTools:**
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка существует и имеет `display: inline-flex`
- [ ] Elements → проверьте `parentNode` кнопки (должен быть `#theme-toggle-container`)
- [ ] Console → проверьте логи: `[HUD][ThemeButton] ensureThemeButton result` после каждого цикла
- [ ] Console → убедитесь, что нет ошибок

---

### Сценарий 3: HUD-режим

#### 3.1. Открытие HUD (клик по метке в 3D)

**Шаги:**
1. Дождитесь загрузки 3D-сцены
2. Кликните по метке узла в 3D-сцене
3. Проверьте открытие HUD-панели

**Ожидаемый результат:**
- [ ] **HUD-панель открывается** (overlay `#hud-overlay` виден)
- [ ] **Контент узла отображается** (заголовок, текст, ссылки)
- [ ] **Кнопка темы (`#theme-toggle-btn`) остаётся видимой** (не скрывается HUD)
- [ ] **Навигация между секциями работает** (если есть несколько секций)
- [ ] **Закрытие HUD (ESC или клик вне панели) работает**

**Проверка в DevTools:**
- [ ] Elements → найдите `#hud-overlay` → убедитесь, что overlay виден
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка видна
- [ ] Console → проверьте логи: `[HUD] Opening panel for node: ...`

#### 3.2. Переключение темы во время открытого HUD

**Шаги:**
1. Откройте HUD (клик по метке)
2. Переключите тему (кнопка темы)
3. Проверьте состояние HUD и темы

**Ожидаемый результат:**
- [ ] **Тема переключается** (Dark ↔ Light)
- [ ] **HUD остаётся открытым** (не закрывается при смене темы)
- [ ] **Цвета HUD обновляются** (фон, текст, кнопки соответствуют новой теме)
- [ ] **3D-сцена обновляется** (фон сцены, цвета узлов соответствуют новой теме)
- [ ] **Кнопка темы работает** (можно переключить обратно)

**Проверка в DevTools:**
- [ ] Elements → `<html>` → проверьте классы `theme-dark` или `theme-light`
- [ ] Elements → найдите `#hud-overlay` → убедитесь, что overlay виден
- [ ] Elements → проверьте CSS-переменные в HUD (должны обновиться под новую тему)
- [ ] Console → проверьте логи: `[ThemeController] Applying theme: ...`

---

### Сценарий 4: Переключение темы (dark/light) в разных состояниях

#### 4.1. Переключение темы: Тур закрыт, HUD закрыт

**Шаги:**
1. Убедитесь, что тур закрыт и HUD закрыт
2. Переключите тему (кнопка темы)
3. Проверьте состояние

**Ожидаемый результат:**
- [ ] **Тема переключается** (Dark ↔ Light)
- [ ] **Кнопка темы работает** (можно переключить обратно)
- [ ] **3D-сцена обновляется** (фон, цвета узлов, сетка)
- [ ] **UI обновляется** (хедер, футер, кнопки)
- [ ] **localStorage обновляется** (`colorTheme` сохраняется)

**Проверка в DevTools:**
- [ ] Elements → `<html>` → проверьте классы `theme-dark` или `theme-light`
- [ ] Application → Local Storage → проверьте `colorTheme`
- [ ] Console → проверьте логи: `[ThemeController] Applying theme: ...`

#### 4.2. Переключение темы: Тур открыт, HUD закрыт

**Шаги:**
1. Откройте тур (кнопка "Демо" или автозапуск)
2. Переключите тему (кнопка темы)
3. Проверьте состояние

**Ожидаемый результат:**
- [ ] **Тема переключается** (Dark ↔ Light)
- [ ] **Тур остаётся открытым** (не закрывается при смене темы)
- [ ] **Цвета тура обновляются** (фон, текст, кнопки соответствуют новой теме)
- [ ] **Кнопка темы работает** (можно переключить обратно)

**Проверка в DevTools:**
- [ ] Elements → `<html>` → проверьте классы `theme-dark` или `theme-light`
- [ ] Elements → найдите `#tour-overlay` → убедитесь, что overlay виден
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка видна и работает

#### 4.3. Переключение темы: Тур закрыт, HUD открыт

**Шаги:**
1. Откройте HUD (клик по метке)
2. Переключите тему (кнопка темы)
3. Проверьте состояние

**Ожидаемый результат:**
- [ ] **Тема переключается** (Dark ↔ Light)
- [ ] **HUD остаётся открытым** (не закрывается при смене темы)
- [ ] **Цвета HUD обновляются** (фон, текст, кнопки соответствуют новой теме)
- [ ] **3D-сцена обновляется** (фон, цвета узлов)
- [ ] **Кнопка темы работает** (можно переключить обратно)

**Проверка в DevTools:**
- [ ] Elements → `<html>` → проверьте классы `theme-dark` или `theme-light`
- [ ] Elements → найдите `#hud-overlay` → убедитесь, что overlay виден
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка видна и работает

#### 4.4. Переключение темы: Тур открыт, HUD открыт

**Шаги:**
1. Откройте тур
2. Откройте HUD (клик по метке в 3D)
3. Переключите тему (кнопка темы)
4. Проверьте состояние

**Ожидаемый результат:**
- [ ] **Тема переключается** (Dark ↔ Light)
- [ ] **Тур остаётся открытым** (не закрывается при смене темы)
- [ ] **HUD остаётся открытым** (не закрывается при смене темы)
- [ ] **Цвета тура и HUD обновляются** (соответствуют новой теме)
- [ ] **Кнопка темы работает** (можно переключить обратно)

**Проверка в DevTools:**
- [ ] Elements → `<html>` → проверьте классы `theme-dark` или `theme-light`
- [ ] Elements → найдите `#tour-overlay` → убедитесь, что overlay виден
- [ ] Elements → найдите `#hud-overlay` → убедитесь, что overlay виден
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка видна и работает

---

### Сценарий 5: Взаимодействие с 3D-сценой

#### 5.1. Выбор узла в 3D-сцене

**Шаги:**
1. Дождитесь загрузки 3D-сцены
2. Кликните по узлу в 3D-сцене (или по метке)
3. Проверьте открытие HUD

**Ожидаемый результат:**
- [ ] **HUD открывается** (overlay `#hud-overlay` виден)
- [ ] **Контент узла отображается** (заголовок, текст, ссылки)
- [ ] **3D-сцена остаётся интерактивной** (можно прокручивать, камера движется)
- [ ] **Метки узлов остаются видимыми** (не скрываются при открытом HUD)

**Проверка в DevTools:**
- [ ] Elements → найдите `#hud-overlay` → убедитесь, что overlay виден
- [ ] Console → проверьте логи: `[HUD] Opening panel for node: ...`
- [ ] Console → проверьте логи: `[Picking] Node selected: ...`

#### 5.2. Открытие HUD из разных узлов

**Шаги:**
1. Откройте HUD для одного узла
2. Закройте HUD (ESC)
3. Откройте HUD для другого узла
4. Проверьте состояние

**Ожидаемый результат:**
- [ ] **HUD открывается для каждого узла** (контент соответствует выбранному узлу)
- [ ] **Кнопка темы (`#theme-toggle-btn`) остаётся видимой** при всех переключениях
- [ ] **Нет ошибок в консоли** при переключении между узлами

**Проверка в DevTools:**
- [ ] Elements → найдите `#hud-overlay` → убедитесь, что контент обновляется
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка всегда видна
- [ ] Console → проверьте логи: `[HUD] Opening panel for node: ...` для каждого узла

---

### Сценарий 6: Работа прелоадера

#### 6.1. Отображение прелоадера при загрузке

**Шаги:**
1. Откройте сайт в режиме инкогнито (или очистите кеш)
2. Откройте DevTools → Network → установите "Slow 3G"
3. Перезагрузите страницу
4. Проверьте отображение прелоадера

**Ожидаемый результат:**
- [ ] **Прелоадер отображается** (`#preloader-overlay` виден)
- [ ] **Прелоадер загружается в iframe** (`ux4ai-preloader-loop-outline-noise.html`)
- [ ] **Прелоадер скрывается** после инициализации 3D-сцены
- [ ] **Нет ошибок загрузки** прелоадера (404, network errors)

**Проверка в DevTools:**
- [ ] Elements → найдите `#preloader-overlay` → убедитесь, что overlay виден при загрузке
- [ ] Elements → найдите `#preloader-frame` → убедитесь, что iframe загружается
- [ ] Network → проверьте загрузку `ux4ai-preloader-loop-outline-noise.html` (статус 200)
- [ ] Console → проверьте логи: `[Bootstrap] Hiding preloader`

#### 6.2. Скрытие прелоадера после загрузки

**Шаги:**
1. Дождитесь полной загрузки страницы
2. Проверьте состояние прелоадера

**Ожидаемый результат:**
- [ ] **Прелоадер скрыт** (`#preloader-overlay` имеет класс `hidden`)
- [ ] **3D-сцена видна** (рендерится)
- [ ] **Хедер виден** (кнопки доступны)
- [ ] **Кнопка темы (`#theme-toggle-btn`) видна** и работает

**Проверка в DevTools:**
- [ ] Elements → найдите `#preloader-overlay` → убедитесь, что overlay скрыт (класс `hidden`)
- [ ] Elements → найдите `#theme-toggle-btn` → убедитесь, что кнопка видна

---

### Сценарий 7: Запуск с изменённым базовым путём (Фаза B)

#### 7.1. Запуск сервера из папки UX4AI

**Шаги:**
1. Откройте терминал в папке `UX4AI/`
2. Запустите сервер: `npx serve -l 5173 .`
3. Откройте `http://localhost:5173/` в браузере
4. Откройте DevTools → Console

**Ожидаемый результат:**
- [ ] **Базовый путь = ''** (в консоли: `[Bootstrap] UX4AI base path computed: `)
- [ ] **Прелоадер загружается** (`/ux4ai-preloader-loop-outline-noise.html` - статус 200)
- [ ] **Модули загружаются** (`/core/engine.js`, `/ui/themeSwitcher.js` - статус 200)
- [ ] **Нет ошибок 404** в Network tab
- [ ] **Все функции работают** (тур, тема, HUD)

**Проверка в DevTools:**
- [ ] Console → проверьте: `[Bootstrap] UX4AI base path computed: ` (пустая строка)
- [ ] Console → проверьте: `[Bootstrap] Preloader path set: /ux4ai-preloader-loop-outline-noise.html`
- [ ] Network → проверьте, что все модули загружаются с корневого пути (`/core/...`, `/ui/...`)

#### 7.2. Запуск сервера из родительского каталога

**Шаги:**
1. Откройте терминал в родительском каталоге (на уровень выше `UX4AI/`)
2. Запустите сервер: `npx serve -l 5173 .`
3. Откройте `http://localhost:5173/UX4AI/` в браузере
4. Откройте DevTools → Console

**Ожидаемый результат:**
- [ ] **Базовый путь = '/UX4AI'** (в консоли: `[Bootstrap] UX4AI base path computed: /UX4AI`)
- [ ] **Прелоадер загружается** (`/UX4AI/ux4ai-preloader-loop-outline-noise.html` - статус 200)
- [ ] **Модули загружаются** (`/UX4AI/core/engine.js`, `/UX4AI/ui/themeSwitcher.js` - статус 200)
- [ ] **Нет ошибок 404** в Network tab
- [ ] **Все функции работают** (тур, тема, HUD)

**Проверка в DevTools:**
- [ ] Console → проверьте: `[Bootstrap] UX4AI base path computed: /UX4AI`
- [ ] Console → проверьте: `[Bootstrap] Preloader path set: /UX4AI/ux4ai-preloader-loop-outline-noise.html`
- [ ] Network → проверьте, что все модули загружаются с префиксом `/UX4AI/` (`/UX4AI/core/...`, `/UX4AI/ui/...`)

#### 7.3. Проверка динамических импортов

**Шаги:**
1. Запустите сервер из любого каталога (UX4AI или родительский)
2. Откройте DevTools → Console
3. Проверьте загрузку модулей

**Ожидаемый результат:**
- [ ] **Все импорты используют базовый путь** (нет жёстко закодированных `./`)
- [ ] **`window.__UX4AI_BASE_PATH__` определён** до загрузки модулей
- [ ] **Прелоадер получает правильный путь** до загрузки модулей
- [ ] **Нет ошибок импорта** в консоли

**Проверка в DevTools:**
- [ ] Console → выполните: `window.__UX4AI_BASE_PATH__` (должен вернуть `''` или `'/UX4AI'`)
- [ ] Network → проверьте, что все динамические импорты используют правильный путь
- [ ] Console → проверьте отсутствие ошибок типа "Failed to load module"

#### 7.4. Проверка глобальных функций

**Шаги:**
1. Запустите сайт (из любого каталога)
2. Откройте DevTools → Console
3. Проверьте доступность глобальных функций

**Ожидаемый результат:**
- [ ] **`window.getCurrentThemeId` доступна** (функция, не undefined)
- [ ] **`window.toggleTheme` доступна** (функция, не undefined)
- [ ] **`window.tour_reset` доступна** (функция, не undefined)
- [ ] **`window.startTour` доступна** (функция, не undefined)

**Проверка в DevTools:**
- [ ] Console → выполните: `typeof window.getCurrentThemeId` (должно быть `'function'`)
- [ ] Console → выполните: `typeof window.toggleTheme` (должно быть `'function'`)
- [ ] Console → выполните: `typeof window.tour_reset` (должно быть `'function'`)

---

## 🚀 Быстрая проверка (5 минут)

### 1. Запустить локальный сервер

```bash
# Python
python -m http.server 8000

# Или Node.js
npx http-server -p 8000
```

Откройте: `http://localhost:8000`

### 2. Базовая проверка функциональности

- [ ] **Страница загружается** без ошибок в консоли
- [ ] **3D-сцена рендерится** (видно 3D-пространство)
- [ ] **Хедер отображается** (Владимир Костял, кнопки)
- [ ] **Тур запускается** (кнопка "Демо" или автоматически)
- [ ] **HUD открывается** (клик по метке в 3D)
- [ ] **Темы переключаются** (кнопка темы в хедере)

---

## 🔍 Детальная проверка (15 минут)

### 3. Service Worker

**DevTools → Application → Service Workers**

- [ ] Видно: `sw.js` со статусом **"activated and running"**
- [ ] В Console: `[SW] Registered: http://localhost:8000/`
- [ ] В Console: `[SW] Service Worker loaded, version: 1.0.0`

**Проверка кеширования:**
- [ ] DevTools → Application → Cache Storage
- [ ] Видно кеш: `ux4ai-v1.0.0`
- [ ] В кеше есть файлы: `index.html`, `tokens.css`, `main.css`

**Проверка обновлений:**
1. Измените `BUILD_VERSION` в `sw.js` на `1.0.1`
2. Перезагрузите страницу
3. Должен появиться **баннер "Доступна новая версия"**
4. Нажмите "Обновить" → страница перезагрузится

### 4. Lazy-load изображений

**DevTools → Network → Img**

- [ ] При первой загрузке загружаются только видимые изображения
- [ ] Прокрутите страницу вниз
- [ ] Новые изображения загружаются по мере появления в viewport
- [ ] В атрибутах `<img>` есть `loading="lazy"`

**Проверка:**
- Откройте Network tab
- Установите фильтр "Img"
- Прокрутите страницу
- Изображения должны загружаться постепенно

### 5. No-JS Fallback

**DevTools → Settings (⚙️) → Debugger → Disable JavaScript**

- [ ] Перезагрузите страницу (Ctrl+R)
- [ ] Должно показаться **сообщение**: "Интерактивные элементы требуют JavaScript"
- [ ] Видна кнопка **"Скачать резюме (PDF)"**
- [ ] Есть ссылка на Telegram

**Вернуть JS:**
- DevTools → Settings → Debugger → **снимите** "Disable JavaScript"
- Перезагрузите страницу

### 6. CSS-токены

**DevTools → Elements → выберите любой элемент**

- [ ] В Styles видно использование `var(--Color/...)`
- [ ] Нет hardcoded цветов типа `#5B9CFF` или `rgba(91, 156, 255, ...)`
- [ ] Используются токены: `var(--Color/Accent/Primary)`, `var(--Spacing/...)`

**Проверка переменных:**
- DevTools → Elements → `<html>` элемент
- В Styles найдите `:root`
- Должны быть видны все токены из `tokens.css`

### 7. Мобильная версия

**DevTools → Toggle device toolbar (Ctrl+Shift+M)**

**Выберите устройство:** iPhone 12 или Pixel 5

**Проверки:**
- [ ] **Нет горизонтального скролла** (overflow-x: hidden)
- [ ] **Кнопки во всю ширину** (width: 100%)
- [ ] **Кнопки имеют min-height: 44px** (Apple HIG)
- [ ] **Тур открывается корректно** (карточка на всю ширину)
- [ ] **HUD панели адаптируются** (вертикальный стек)
- [ ] **Интерактивы (TTFT) на 100% ширины**
- [ ] **Текст не обрезается** (word-wrap: break-word)

**Проверка тач-взаимодействий:**
- [ ] Кнопки реагируют мгновенно (нет задержки)
- [ ] Нет выделения текста при тапе
- [ ] Плавная прокрутка

### 8. Визуальная идентичность

**Сравнение с оригиналом:**

- [ ] **Цвета идентичны** (акцент #5B9CFF, фон #0B0F14)
- [ ] **Шрифты те же** (ui-sans-serif, system-ui)
- [ ] **Размеры элементов** не изменились
- [ ] **Анимации работают** (переходы, hover-эффекты)
- [ ] **3D-сцена выглядит так же**

---

## 📊 Lighthouse-аудит (10 минут)

### Установка Lighthouse

```bash
npm install -g lighthouse
```

### Запуск аудита

```bash
lighthouse http://localhost:8000 --view
```

### Целевые метрики

| Метрика | Цель | Проверка |
|---------|------|----------|
| **Performance** | ≥ 85 | [ ] |
| **Accessibility** | ≥ 90 | [ ] |
| **Best Practices** | ≥ 90 | [ ] |
| **SEO** | ≥ 90 | [ ] |
| **CLS** | < 0.1 | [ ] |
| **LCP** | < 2.5s | [ ] |
| **FID** | < 100ms | [ ] |

### Что проверить в отчёте

- [ ] **LCP (Largest Contentful Paint)** — должен быть < 2.5s
- [ ] **CLS (Cumulative Layout Shift)** — должен быть < 0.1
- [ ] **FCP (First Contentful Paint)** — должен быть < 1.8s
- [ ] **TTI (Time to Interactive)** — должен быть < 3.8s
- [ ] **Total Blocking Time** — должен быть < 200ms

**Обратите внимание на:**
- ✅ "Uses efficient cache policies" (Service Worker)
- ✅ "Image elements have explicit width and height" (lazy-load)
- ✅ "Document has a valid `lang` attribute"
- ✅ "Links are crawlable"

---

## 🐛 Проверка ошибок

### Console (DevTools → Console)

- [ ] **Нет ошибок** (красные сообщения)
- [ ] Видны логи: `[SW] Registered`, `[Bootstrap] ✅ Ready!`
- [ ] Нет предупреждений о загрузке ресурсов

### Network (DevTools → Network)

- [ ] Все ресурсы загружаются (статус 200)
- [ ] Нет 404 ошибок
- [ ] `tokens.css` и `main.css` загружаются
- [ ] `sw.js` загружается

### Application (DevTools → Application)

- [ ] **Service Workers** — активен
- [ ] **Cache Storage** — есть кеш `ux4ai-v1.0.0`
- [ ] **Storage** — нет ошибок

---

## 🎯 Функциональные тесты

### Тур (Guided Tour)

- [ ] **Автозапуск** работает (если не был завершён ранее)
- [ ] **startTour() возвращает true** при наличии шага 1 и DOM элементов
- [ ] **startTour() возвращает false** и логирует причину при отсутствии шага 1 или DOM элементов
- [ ] **Кнопки "Назад/Далее"** работают
- [ ] **Ссылки в туре** открываются в новой вкладке
- [ ] **Интерактивы (TTFT)** загружаются и работают
- [ ] **Закрытие тура** (ESC или кнопка) работает
- [ ] **Перезапуск тура** работает без перезагрузки страницы
- [ ] **Кнопка "Перезапустить тур"** всегда работает

### HUD (Heads-Up Display)

- [ ] **Клик по метке** открывает HUD
- [ ] **Панели отображаются** корректно
- [ ] **Навигация между секциями** работает
- [ ] **Закрытие HUD** (ESC или клик вне панели) работает
- [ ] **Ссылки в HUD** открываются корректно

### 3D-сцена

- [ ] **Камера движется** при прокрутке
- [ ] **Метки отображаются** в правильных позициях
- [ ] **Hover-эффекты** на метках работают
- [ ] **Орбитальный режим** (если есть) работает

### Темы

- [ ] **Переключение темы** работает
- [ ] **Calm тема** применяется корректно
- [ ] **Matrix тема** применяется корректно
- [ ] **Токены обновляются** при смене темы

---

## 🎯 Theme & Tour Stability Testing

### Матрица проверок (8 комбинаций)

Проверьте все комбинации состояний:

| Тема | Состояние тура | HUD | Кнопка темы видна | Кнопка темы работает |
|------|----------------|-----|-------------------|----------------------|
| Light | Закрыт | Закрыт | [ ] | [ ] |
| Light | Закрыт | Открыт | [ ] | [ ] |
| Light | Открыт | Закрыт | [ ] | [ ] |
| Light | Открыт | Открыт | [ ] | [ ] |
| Dark | Закрыт | Закрыт | [ ] | [ ] |
| Dark | Закрыт | Открыт | [ ] | [ ] |
| Dark | Открыт | Закрыт | [ ] | [ ] |
| Dark | Открыт | Открыт | [ ] | [ ] |

**Как проверить:**
1. Установите тему (Light/Dark)
2. Установите состояние тура (открыт/закрыт)
3. Установите состояние HUD (открыт/закрыт)
4. Проверьте видимость кнопки темы (`#theme-toggle-btn`)
5. Проверьте работоспособность кнопки темы (клик переключает тему)

### Тест стабильности (5 циклов)

Выполните **5 циклов** следующей последовательности:

**Цикл:**
1. **Перезапуск тура** (кнопка "Перезапустить тур" или `window.tour_reset()`)
2. **Смена темы** (кнопка "Тема" или `window.hud.ensureThemeButton()`)
3. **Закрытие тура** (ESC или кнопка закрытия)

**Проверки после каждого цикла:**

- [ ] **Цикл 1:** Кнопка темы видна и работает
- [ ] **Цикл 2:** Кнопка темы видна и работает
- [ ] **Цикл 3:** Кнопка темы видна и работает
- [ ] **Цикл 4:** Кнопка темы видна и работает
- [ ] **Цикл 5:** Кнопка темы видна и работает

**Ожидаемый результат:**
- ✅ Кнопка темы (`#theme-toggle-btn`) **всегда отображается** после каждого шага
- ✅ Кнопка темы **всегда работает** (переключает тему при клике)
- ✅ Кнопка темы **не исчезает** и **не перемещается** в DOM
- ✅ Нет ошибок в консоли браузера

**Проверка в DevTools:**
- [ ] Elements → найдите `#theme-toggle-btn`
- [ ] Убедитесь, что кнопка существует и имеет `display: inline-flex`
- [ ] Проверьте, что `parentNode` — это `#theme-toggle-container`
- [ ] Проверьте, что кнопка имеет обработчики событий (Event Listeners)

**Логирование:**
- [ ] В консоли видны логи: `[HUD][ThemeButton] created`
- [ ] В консоли видны логи: `[HUD][ThemeButton] ensureThemeButton result`
- [ ] В консоли видны логи: `[Bootstrap] ensureThemeButton saved to window.hud.ensureThemeButton`
- [ ] После каждого цикла проверьте, что кнопка темы стабильна

**Известные проблемы:**
- Если кнопка темы исчезает — проверьте логи в консоли
- Если кнопка темы не работает — проверьте, что `window.hud.ensureThemeButton` существует
- Если кнопка темы перемещается — проверьте, что тур не изменяет `parentNode`

---

## 📱 Тестирование на реальном устройстве

### Подготовка

1. Узнайте IP компьютера:
   ```bash
   # Windows
   ipconfig
   # Mac/Linux
   ifconfig
   ```

2. На телефоне откройте: `http://[IP]:8000`

### Проверки на мобильном

- [ ] **Страница загружается** без ошибок
- [ ] **Нет горизонтального скролла**
- [ ] **Кнопки легко нажимаются** (min-height 44px)
- [ ] **Тур открывается** корректно
- [ ] **3D-сцена рендерится** (может быть медленнее)
- [ ] **Тач-жесты** работают плавно
- [ ] **Service Worker** регистрируется (проверить в мобильном Chrome DevTools)

---

## ✅ Финальный чеклист

### Перед мерджем в main:

- [ ] Все функциональные тесты пройдены
- [ ] Lighthouse Performance ≥ 85
- [ ] Lighthouse Accessibility ≥ 90
- [ ] Нет ошибок в Console
- [ ] Мобильная версия работает корректно
- [ ] Service Worker работает
- [ ] Lazy-load работает
- [ ] No-JS fallback работает
- [ ] Визуально идентичен оригиналу

---

## 🚨 Известные проблемы и решения

### Service Worker не регистрируется

**Решение:**
- Убедитесь, что сайт открыт через `localhost` или `https`
- DevTools → Application → Clear site data
- Перезагрузите страницу

### Lazy-load не работает

**Решение:**
- Проверьте браузер (должен быть современный: Chrome 76+, Firefox 75+)
- Fallback: изображения загрузятся при прокрутке

### Баннер обновления не показывается

**Решение:**
- Измените `BUILD_VERSION` в `sw.js`
- Hard reload (Ctrl+Shift+R)

### Мобильная версия не адаптируется

**Решение:**
- Проверьте, что `main.css` загружается
- DevTools → Elements → проверьте применённые стили
- Убедитесь, что media queries работают

---

## 📝 Заметки для тестирования

**Дата тестирования:** _______________  
**Тестировщик:** _______________  
**Браузер:** _______________  
**Версия:** _______________

**Найденные проблемы:**
1. 
2. 
3. 

**Рекомендации:**
1. 
2. 
3. 

---

*Чеклист создан для рефакторинга версии 1.0.0*

