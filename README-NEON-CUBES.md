# ⚡ Неоновая тема с кубами и голографическими текстурами

## 🎯 Что изменено

Для **неоновой темы** теперь используется специальная 3D сцена с:

### 🔲 Кубы вместо сфер
- **Узлы** теперь представлены **кубами** вместо сфер
- **Размер**: 0.3x0.3x0.3 единицы
- **Анимация**: каждый куб вращается вокруг случайной оси с разной скоростью

### 🌐 Окружающий куб
- **Большой куб** (20x20x20 единиц) окружает всю сцену
- **Прозрачный** с голографическими текстурами
- **Анимация**: медленное вращение по всем осям

### 🎨 Голографические текстуры
- **Эффект облаков** - фрактальный шум для создания облачных паттернов
- **Голографические полосы** - движущиеся линии как в голограммах
- **Свечение по краям** - яркие края для эффекта свечения
- **Анимация** - все текстуры анимированы во времени

## 🔧 Технические детали

### Шейдеры
Используются **кастомные шейдеры** для создания эффектов:

#### Вертексный шейдер:
```glsl
varying vec2 vUv;
varying vec3 vPosition;
varying vec3 vNormal;
void main() {
  vUv = uv;
  vPosition = position;
  vNormal = normal;
  gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
}
```

#### Фрагментный шейдер:
```glsl
// Шум для облаков
float noise(vec3 p) {
  return fract(sin(dot(p, vec3(12.9898, 78.233, 45.164))) * 43758.5453);
}

// Фрактальный шум
float fbm(vec3 p) {
  float value = 0.0;
  float amplitude = 0.5;
  float frequency = 1.0;
  
  for (int i = 0; i < 4; i++) {
    value += amplitude * noise(p * frequency);
    amplitude *= 0.5;
    frequency *= 2.0;
  }
  
  return value;
}

// Основной эффект
void main() {
  vec3 pos = vPosition * 2.0 + time * 0.1;
  
  // Облачный эффект
  float cloud = fbm(pos);
  cloud = smoothstep(0.3, 0.7, cloud);
  
  // Голографический эффект
  float hologram = sin(vUv.x * 20.0 + time * 2.0) * 0.5 + 0.5;
  hologram *= sin(vUv.y * 15.0 + time * 1.5) * 0.5 + 0.5;
  
  // Комбинируем эффекты
  float finalOpacity = cloud * hologram * opacity;
  
  // Свечение по краям
  float edge = 1.0 - smoothstep(0.0, 0.1, min(min(vUv.x, vUv.y), min(1.0 - vUv.x, 1.0 - vUv.y)));
  finalOpacity += edge * 0.3;
  
  gl_FragColor = vec4(color, finalOpacity);
}
```

### Анимация
- **Время** передается в шейдеры для анимации
- **Кубы узлов** вращаются вокруг случайных осей
- **Окружающий куб** медленно вращается по всем осям
- **Текстуры** анимированы с разной скоростью

## 🎨 Настройка эффектов

### Изменение скорости анимации:
В файле `index-working.html` найдите метод `updateNeonScene`:

```javascript
// Скорость вращения кубов
cube.userData.rotationSpeed = 0.01 + Math.random() * 0.02;

// Скорость вращения окружающего куба
window.themeManager.surroundingCube.rotation.x += 0.001;
window.themeManager.surroundingCube.rotation.y += 0.002;
window.themeManager.surroundingCube.rotation.z += 0.0005;
```

### Изменение параметров шейдеров:
В методе `createHolographicMaterials`:

```javascript
uniforms: {
  time: { value: 0 },
  color: { value: new THREE.Color(theme.impulseColor) },
  opacity: { value: 0.8 }  // ← Измените прозрачность
}
```

### Изменение эффектов в шейдерах:
В фрагментном шейдере:

```glsl
// Частота голографических полос
float hologram = sin(vUv.x * 20.0 + time * 2.0) * 0.5 + 0.5;  // ← Измените 20.0
hologram *= sin(vUv.y * 15.0 + time * 1.5) * 0.5 + 0.5;       // ← Измените 15.0

// Интенсивность облаков
float cloud = fbm(pos);
cloud = smoothstep(0.3, 0.7, cloud);  // ← Измените 0.3 и 0.7

// Свечение по краям
float edge = 1.0 - smoothstep(0.0, 0.1, ...);  // ← Измените 0.1
finalOpacity += edge * 0.3;  // ← Измените 0.3
```

## 🚀 Как использовать

1. **Откройте** `index-working.html`
2. **Переключитесь** на неоновую тему (⚡ Неоновая)
3. **Наслаждайтесь** кубами с голографическими текстурами!

## 🎯 Результат

Теперь неоновая тема имеет:
- ✅ **Кубы** вместо сфер для узлов
- ✅ **Голографические текстуры** с эффектом облаков
- ✅ **Окружающий куб** с анимированными текстурами
- ✅ **Плавная анимация** всех элементов
- ✅ **Уникальный киберпанк стиль**

**Наслаждайтесь новой неоновой 3D сценой!** ⚡🔲
