# 🎨 Руководство по настройке тем

**Версия:** Фаза 5 — Обновлено под новую систему  
**Дата создания:** 2024  
**Последнее обновление:** Фаза 5 — Закрытие двойной правды

---

## ⚠️ ВАЖНО: Единственный источник настройки тем

**Этот документ (`THEME-CUSTOMIZATION-GUIDE.md`) — единственный источник руководства по настройке тем.**

**Связанные документы:**
- **[THEME-SYSTEM.md](THEME-SYSTEM.md)** — архитектура системы тем
- **[docs/COLOR_MAP.md](docs/COLOR_MAP.md)** — документация всех цветовых токенов

---

## 📋 Обзор системы

Проект использует **централизованную систему тем**, которая позволяет легко изменять:
- 🎨 **Цвета интерфейса** — все элементы UI
- 🌟 **3D сцену** — узлы, лучи, освещение, материалы

**Источники истины:**
- **Runtime:** `core/theme-colors.js` — единственный источник цветов
- **Документация:** `docs/COLOR_MAP.md` — описание всех токенов

## 🚀 Быстрый старт

### 1. Откройте файл с цветами
```bash
core/theme-colors.js
```

### 2. Найдите нужную тему
```javascript
const darkTheme = {
  // ... токены темной темы
};

const lightTheme = {
  // ... токены светлой темы
};
```

### 3. Измените параметры
```javascript
const darkTheme = {
  bgBase: '#0D1117',  // ← Измените цвет фона
  accentPrimary: '#58A6FF' // ← Измените акцентный цвет
};
```

**⚠️ Важно:** После изменения цветов обновите документацию в `docs/COLOR_MAP.md`!

## 🎨 Настройка цветов интерфейса

### Основные цвета
```javascript
colors: {
  // Фоны
  bgPrimary: '#0B0F14',        // Основной фон
  bgSecondary: '#121722',      // Вторичный фон
  bgTertiary: '#162032',       // Третичный фон
  bgOverlay: 'rgba(18, 23, 34, 0.95)', // Overlay фон
  bgHeader: 'rgba(18, 23, 34, 0.30)',  // Фон шапки
  bgHud: 'rgba(18, 23, 34, 0.90)',     // Фон HUD
  bgPanel: 'rgba(18, 23, 34, 0.90)',   // Фон панелей
  
  // Текст
  textPrimary: '#E6EEF8',      // Основной текст
  textSecondary: '#CFE8FF',    // Вторичный текст
  textTertiary: '#9AA6B2',     // Третичный текст
  textMuted: 'rgba(154, 166, 178, 0.7)', // Приглушенный текст
  
  // Границы и акценты
  borderPrimary: '#243041',    // Основные границы
  borderSecondary: '#2a3b51',  // Вторичные границы
  accentPrimary: '#5B9CFF',    // Основной акцент
  accentSecondary: 'rgba(91, 156, 255, 0.3)', // Вторичный акцент
  
  // Тени
  shadowPrimary: 'rgba(0, 0, 0, 0.25)',     // Основные тени
  shadowAccent: 'rgba(91, 156, 255, 0.5)',  // Акцентные тени
  shadowInset: 'rgba(36, 48, 65, 0.4)',     // Внутренние тени
  
  // Overlay
  overlayBg: 'rgba(0, 0, 0, 0.45)' // Фон overlay
}
```

## 🌟 Настройка 3D сцены

### Основные параметры
```javascript
scene3D: {
  // Фон сцены
  background: '#0B0F14',
  
  // Цвета узлов (массив из 8 цветов)
  nodeColors: [
    '#5B9CFF', '#22C55E', '#F59E0B', '#EF4444',
    '#8B5CF6', '#14B8A6', '#E11D48', '#A3E635'
  ],
  
  // Цвет импульсов
  impulseColor: '#5B9CFF',
  
  // Материалы узлов
  nodeMaterial: {
    emissiveIntensity: 0.2,  // Интенсивность свечения (0-1)
    shininess: 100,          // Блеск (0-1000)
    specular: '#ffffff'      // Цвет бликов
  },
  
  // Материалы лучей
  beamMaterial: {
    glow: 0.03,              // Свечение лучей (0-1)
    packetWidth: 0.15,       // Ширина импульса (0-1)
    opacity: 0.8             // Прозрачность (0-1)
  },
  
  // Освещение
  lighting: {
    ambient: {
      color: '#404040',      // Цвет окружающего света
      intensity: 0.3         // Интенсивность (0-2)
    },
    directional: {
      color: '#ffffff',      // Цвет направленного света
      intensity: 0.8,        // Интенсивность (0-2)
      position: { x: 10, y: 10, z: 5 } // Позиция источника
    }
  }
}
```

## 📷 Настройка поведения камеры

### Основные параметры
```javascript
camera: {
  // Начальная позиция камеры
  initialPosition: { x: 0, y: 1.2, z: 12 },
  
  // Скорость скролла (чем больше, тем быстрее)
  scrollSpeed: 0.5,
  
  // Плавность анимации
  animationEasing: 'easeOutCubic', // easeIn, easeOut, easeInOut, linear
  
  // Ограничения движения
  limits: {
    minY: -5,    // Минимальная высота
    maxY: 10,    // Максимальная высота
    minZ: 2,     // Минимальное расстояние
    maxZ: 20     // Максимальное расстояние
  },
  
  // Орбитальный режим
  orbit: {
    minRadius: 1.6,        // Минимальный радиус орбиты
    maxRadius: 12.0,       // Максимальный радиус орбиты
    rotateSpeed: 0.006,    // Скорость вращения
    zoomSpeed: 0.0025      // Скорость зума
  }
}
```

## ✨ Создание новой темы

> ⚠️ **ВАЖНО:** В текущей версии проекта поддерживаются только Dark и Light темы. Добавление новых тем требует изменений в нескольких местах.

### 1. Добавьте тему в `core/theme-colors.js`

```javascript
// В файле core/theme-colors.js
const PALETTE = {
  // ... существующие цвета
  myThemeBg: '#1a1a2e',
  myThemeText: '#ffffff',
  myThemeAccent: '#ff6b6b',
  // ... остальные цвета
};

// Добавьте новую тему
const myTheme = {
  // UI цвета
  bgBase: PALETTE.myThemeBg,
  textPrimary: PALETTE.myThemeText,
  accentPrimary: PALETTE.myThemeAccent,
  // ... остальные токены
};

// 3D сцена
const myThemeScene = {
  background: PALETTE.myThemeBg,
  nodePalette: ['#ff6b6b', '#4ecdc4', '#45b7d1', '#96ceb4'],
  impulseColor: PALETTE.myThemeAccent,
  // ... остальные настройки
};
```

### 2. Добавьте конфигурацию в `themes/ui-theme-config.js`

```javascript
// В файле themes/ui-theme-config.js
export const MY_THEME_CONFIG = {
  id: 'my-theme',
  name: 'Моя тема',
  cssClasses: {
    html: ['theme-my-theme'],
    body: ['theme-my-theme']
  },
  sceneColors: null, // Заполняется из getSceneColors()
  get metaThemeColor() {
    const colors = getThemeColorsByName('my-theme');
    return colors.bgBase;
  }
};
```

### 3. Добавьте CSS переменные в `styles/tokens.css`

```css
/* В файле styles/tokens.css */
:root.theme-my-theme {
  --Color/MyTheme/Background/Primary: #1a1a2e;
  --Color/MyTheme/Text/Primary: #ffffff;
  --Color/MyTheme/Accent/Primary: #ff6b6b;
  /* ... остальные переменные */
}
```

### 4. Обновите документацию

После добавления новой темы обновите:
- `docs/COLOR_MAP.md` — добавьте описание новых токенов
- `THEME-SYSTEM.md` — обновите описание структуры (если нужно)

---

## ⚠️ Примечание о legacy файлах

**Legacy CSS файлы** (перенесены в `themes/legacy/`):
- `themes/legacy/theme-variables.css` — **НЕ ИСПОЛЬЗУЕТСЯ**, готов к удалению
- `themes/legacy/simple-themes.css` — **НЕ ИСПОЛЬЗУЕТСЯ**, готов к удалению

Эти файлы не используются в текущей версии проекта. Все цвета определены в `core/theme-colors.js` и `styles/tokens.css`.

---

## 📝 Дополнительные эффекты (опционально)

Если нужно добавить дополнительные эффекты для темы, создайте отдельный модуль:
```javascript
// В файле core/theme-effects.js (или другом модуле)
export function applyMyThemeEffects() {
  // Добавьте специальные эффекты для вашей темы
  const elements = document.querySelectorAll('.label, .hud');
  elements.forEach(element => {
    element.style.animation = 'myThemePulse 3s ease-in-out infinite';
  });
}
```

## 🎯 Примеры готовых тем

### Океанская тема
```javascript
ocean: {
  name: 'Океан',
  colors: {
    bgPrimary: '#001122',
    textPrimary: '#87CEEB',
    accentPrimary: '#00BFFF'
  },
  scene3D: {
    background: '#001122',
    nodeColors: ['#00BFFF', '#20B2AA', '#40E0D0', '#00CED1'],
    impulseColor: '#00BFFF'
  }
}
```

### Лесная тема
```javascript
forest: {
  name: 'Лес',
  colors: {
    bgPrimary: '#0D1B0D',
    textPrimary: '#90EE90',
    accentPrimary: '#00FF7F'
  },
  scene3D: {
    background: '#0D1B0D',
    nodeColors: ['#00FF7F', '#32CD32', '#90EE90', '#98FB98'],
    impulseColor: '#00FF7F'
  }
}
```

### Закатная тема
```javascript
sunset: {
  name: 'Закат',
  colors: {
    bgPrimary: '#1A0D0D',
    textPrimary: '#FFB366',
    accentPrimary: '#FF8C00'
  },
  scene3D: {
    background: '#1A0D0D',
    nodeColors: ['#FF8C00', '#FFD700', '#FFB366', '#CD853F'],
    impulseColor: '#FF8C00'
  }
}
```

## 🔧 Продвинутые настройки

### Анимации и эффекты
```javascript
// Добавьте в конфигурацию темы
effects: {
  // Пульсация элементов
  pulse: {
    enabled: true,
    duration: '2s',
    timing: 'ease-in-out'
  },
  
  // Свечение
  glow: {
    enabled: true,
    intensity: 0.5,
    color: '#00FFFF'
  },
  
  // Партиклы
  particles: {
    enabled: true,
    count: 100,
    color: '#ffffff'
  }
}
```

### Динамические цвета
```javascript
// Цвета, которые меняются со временем
dynamicColors: {
  primary: {
    base: '#5B9CFF',
    animation: 'hue-rotate 10s linear infinite'
  }
}
```

## 📱 Адаптивность

### Настройки для разных устройств
```javascript
responsive: {
  mobile: {
    camera: {
      initialPosition: { x: 0, y: 0.8, z: 8 },
      scrollSpeed: 0.3
    }
  },
  tablet: {
    camera: {
      initialPosition: { x: 0, y: 1.0, z: 10 },
      scrollSpeed: 0.4
    }
  }
}
```

## 🚀 Готовые решения

### Быстрое создание темы по цвету
```javascript
function createThemeFromColor(baseColor, name) {
  return {
    name: name,
    colors: {
      bgPrimary: darken(baseColor, 0.8),
      textPrimary: lighten(baseColor, 0.9),
      accentPrimary: baseColor
    },
    scene3D: {
      background: darken(baseColor, 0.8),
      nodeColors: generatePalette(baseColor, 8),
      impulseColor: baseColor
    }
  };
}

// Использование
const purpleTheme = createThemeFromColor('#8B5CF6', 'Фиолетовая');
```

## 🎉 Результат

Теперь вы можете:
- ✅ **Легко настраивать** любые аспекты темы
- ✅ **Создавать новые темы** за несколько минут
- ✅ **Изменять поведение** 3D сцены и камеры
- ✅ **Добавлять эффекты** и анимации
- ✅ **Адаптировать** под разные устройства

**Наслаждайтесь полной свободой настройки!** 🚀
